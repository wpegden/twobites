# Tasks

<!-- SUPERVISOR_TASKS:START -->
## Supervisor Tasks
- [ ] Prove the target statements presented in `PaperTheorems.lean`.
- [ ] Keep reusable proof infrastructure in separate support files when that yields a cleaner project structure.
- [ ] Maintain `TASKS.md` and `PLAN.md` as the proof frontier moves.
- [ ] Keep sorrys within the configured policy.
- [ ] Do not introduce unapproved axioms.
<!-- SUPERVISOR_TASKS:END -->

## Worker Tasks
- [x] Complete the reduced deterministic-target-gap route with deterministic-bounds embedding wrappers in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_paperDeterministicBounds` now internalizes the routine large/medium/small/huge contribution package from the deterministic Section 4 hypotheses, and the lifted shells `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, and `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now expose that same lighter target-gap interface at fixed-embedding, fixed-set, and final extraction level with only the genuinely graph-dependent `hLossLeTarget`, `SPart`, and projection-cap inputs still quantified over graph pairs.
- [x] Expose the direct deterministic-bounds manuscript `paperRISILossGap` route at fixed-embedding level in `repo/Twobites/IndependentSets.lean`: the new theorem `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` now packages the filtered fixed-embedding mass shell directly over the manuscript loss-gap interface with the routine deterministic large/medium/small/huge contribution bounds already discharged, so later combinatorial lemmas can target that direct route without mentioning the intermediate deterministic target-gap shell.
- [x] Expose the same direct deterministic-bounds manuscript `paperRISILossGap` route at fixed-set level in `repo/Twobites/IndependentSets.lean`: `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` now lifts that direct fixed-embedding shell through the embedding-sum identity for one fixed `I`, so the remaining graph-dependent collapse can target the manuscript loss-gap interface directly at both the fixed-embedding and fixed-set layers.
- [x] Carry the direct deterministic-bounds manuscript `paperRISILossGap` route through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now packages that direct manuscript-loss-gap shell all the way to the witness-existence theorem, so the remaining RI obstruction is again only the genuinely graph-dependent fixed-embedding collapse supplying the loss-gap, `SPart`, and opposite-color projection-cap hypotheses.
- [x] Add the deterministic paper-facing Ramsey monotonicity wrapper in `PaperTheorems.lean` / `Twobites/PaperTheorems.lean`: `triangleFreeRamseyWitness_mono` now shows that a triangle-free Ramsey witness on `n` vertices restricts to one on every `m ≤ n`, so the witness-form `main2` interface has the expected downward-closed behavior while the Ramsey-number-side existence wrapper remains blocked only by the separate global nonemptiness/upper-bound layer.
- [ ] Collapse the fixed-embedding three-pool witness decomposition through the projection-choice split-part wrappers: `section4ProjectionChoiceMassSum_section4F_le_exp_of_splitPartTotalError` now mirrors the older actual-conditioned Section 4 shell directly at the `section4ProjectionChoiceMassSum` layer, and both `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_splitPartTotalError` and the fixed-embedding endpoints `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_uniformPartError`, `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_uniformError`, `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_uniformError_of_revealWitnessBounds`, `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap`, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap_of_paperSection4Bound` specialize that shell to one graph-pair summand in `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_projectionChoiceMass`. The near-one RI loss-gap step is now packaged at every wrapper layer needed by the eventual union bound: there are branch-specific graph-pair theorems `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperNearOneReal_of_blue_le_red_of_two_div_le_loglog_of_paperSection4Bound` / `..._of_red_le_blue_...`, the new merged graph-pair dispatcher `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound`, branch-specific/final merged fixed-embedding shells `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperNearOneReal_of_blue_le_red_of_two_div_le_loglog_of_paperSection4Bound` / `..._of_red_le_blue_...` / `..._of_two_div_le_loglog_...`, and the corresponding fixed-set shells `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperNearOneReal_of_blue_le_red_of_two_div_le_loglog_of_paperSection4Bound` / `..._of_red_le_blue_...` / `..._of_two_div_le_loglog_...`. The wrappers `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound_of_embeddingImageBounds`, `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound_of_embeddingImageBounds`, and `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound_of_embeddingImageBounds_lt_explicitGoodMass` now pull the embedding-only red/blue image bounds, image-sum bound, and near-one loss inequality all the way out of the graph-pair and final-extraction quantifiers, so the remaining fixed-embedding/fixed-set collapse obligations are only the genuinely graph-dependent `SPart` / projection-cap hypotheses. The filtered bridge `goodSurvivingGraphPairActualMassIntegrand_le_if_good_projectionChoiceMassBound` and `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_projectionChoiceMass` keep that finite sum supported only on graph pairs that actually satisfy `GoodEventD ∧ SurvivesAsIndependent I`, while `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_of_le` and `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound` package the immediate pointwise step from those filtered summands to manuscript-scale Section 4 exponents and the fixed-set wrappers `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_by_embedding_of_le` / `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound` lift that shell through the embedding-sum identity for one fixed `I`. The new monotone parameter wrappers `section4ProjectionChoiceMassSum_mono`, `goodSurvivingGraphPairProjectionChoiceMassBound_le_section4ProjectionChoiceMassSum_of_le`, and `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_section4ProjectionChoiceMass_of_le` now expose the filtered graph-pair sum in terms of the three literal natural parameters it depends on: a lower bound for the surviving `remaining` open-pair count and upper bounds for the red/blue opposite-witness projection counts. The final extraction step is now also packaged by `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_lt_explicitGoodMass_of_massBound` and the near-one specializations `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound_lt_explicitGoodMass` / `..._of_embeddingImageBounds_lt_explicitGoodMass`, so the concrete remaining work is to turn the checked three-pool red/blue cover lemmas into explicit upper/lower bounds for those three naturals, then feed the resulting `revealError/largeError/mediumError/smallError/hugeRedError/hugeBlueError` package into the choose-multiplied embedding-sum bound.
- [x] Add the deterministic-target-gap RI shell above the current filtered projection-choice wrappers in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_uniformError_of_paperLargeWitness_of_paperHugeWitness_of_paperDeterministicTargetGap_of_paperSection4Bound`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound` now derive the manuscript target-gap side automatically from the existing deterministic Section 4 bounds, so the live RI blocker has narrowed to the literal graph-dependent loss/count inequality `hLossLeTarget` plus the already-isolated deterministic large/medium/small/huge witness hypotheses.
- [x] Add manuscript-loss-gap bridge wrappers into the stronger deterministic-target-gap shell in `repo/Twobites/IndependentSets.lean`: the new pointwise theorem `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap_via_paperDeterministicTargetGap_of_paperSection4Bound` now converts `paperRISILossGap` directly into the stronger deterministic target-gap endpoint on a single good graph pair via `goodSurvivingGraphPair_section4LossLeTarget_of_paperRISILossGap`, the fixed-embedding/fixed-set shells `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_via_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_via_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` now route through that pointwise bridge while still inheriting the deterministic large/medium/small/huge contribution bounds internally from `GoodEventD`, and the extraction wrapper `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperRISILossGap_via_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` carries the same bridge route all the way to the final paper-facing witness theorem.
- [x] Factor the embedding-only image-cardinality hypotheses out of the deterministic-target-gap RI shell in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds` now package the red/blue image-size bounds per embedding rather than per graph pair, so the remaining fixed-embedding collapse for that shell is only the genuinely graph-dependent `hLossLeTarget`, `hSmallCard`, and opposite-color projection-cap data coming from the checked three-pool witness decomposition.
- [x] Lift the deterministic-target-gap RI shell through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds_lt_explicitGoodMass` now packages the passage from the choose-multiplied fixed-set deterministic-target-gap mass bound to the paper-facing witness existence theorem, so that route is aligned with the near-one extraction shell and the remaining obstruction is purely the graph-dependent fixed-embedding collapse.
- [x] Add reduced-hypothesis fixed-embedding and fixed-set deterministic-target-gap shells in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound` now lift the new pointwise theorem `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound` through the filtered embedding and fixed-set mass shells. These wrappers bypass the old cap-weight interface entirely: above the graph-pair layer they ask only for the literal `hLossLeTarget` inequality together with the large/medium/small bounds and the four huge pair-count hypotheses (red diagonal, blue diagonal, blue cross, red cross).
- [x] Carry the reduced deterministic-target-gap route through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound_lt_explicitGoodMass` now packages that lighter reduced shell all the way to the witness-existence theorem, so later combinatorial lemmas can target the reduced `hLossLeTarget` plus large/medium/small/huge-cross interface without needing any additional endpoint assembly.
- [x] Factor the routine deterministic Section 4 contribution bounds out of the deterministic-target-gap RI route in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperDeterministicTargetGap_of_paperSection4Bound` now derives the large/medium/small/huge contribution bounds internally from `GoodEventD`, and the lifted wrappers `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, and `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperDeterministicTargetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now propagate that simplification through the fixed-embedding, fixed-set, and final extraction shells. The remaining deterministic-target-gap frontier is therefore only the genuinely graph-dependent `hLossLeTarget`, `hSmallCard`, and opposite-color projection-cap data coming from the checked three-pool witness decomposition.
- [x] Factor the embedding-only image-cardinality hypotheses out of the manuscript `paperRISILossGap` RI shell in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds` now let the remaining graph-pair work target the pointwise loss-gap and witness-budget hypotheses directly, without separately carrying red/blue image bounds through every graph-pair quantifier.
- [x] Lift the manuscript `paperRISILossGap` RI shell through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_lt_explicitGoodMass` now packages the passage from the choose-multiplied fixed-set manuscript-loss-gap mass bound to the paper-facing witness existence theorem, so that route is aligned with the near-one and deterministic-target-gap extraction shells and the remaining obstruction is purely the graph-dependent fixed-embedding collapse.
- [x] Extract the exact graph-pair loss-to-target lemma from the heavier RISI endpoint in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_section4LossLeTarget_of_paperRISILossGap` now packages the natural-number inequality `hLossLeTarget` directly from `GoodEventD`, the deterministic large/medium/small/huge witness bounds, and the manuscript loss-gap hypothesis `paperRISILossNat κ ε1 n ≤ f(ℓ_R,\ell_B)`, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap` has been refactored to consume this helper instead of reproving the same loss-to-target arithmetic locally.
- [x] Factor the manuscript near-one loss-gap step out already at graph-pair level in `repo/Twobites/IndependentSets.lean`: the new helpers `graphPair_paperRISILossGap_of_paperNearOneReal_of_blue_le_red_of_two_div_le_loglog`, `..._of_red_le_blue_...`, and the merged dispatcher `graphPair_paperRISILossGap_of_paperNearOneReal_of_two_div_le_loglog` now specialize the existing `ConstructionData.paper_risi_hLossGap...` lemmas directly to one embedding/graph-pair, and the branch-specific near-one pointwise wrappers `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperNearOneReal_of_blue_le_red_of_two_div_le_loglog_of_paperSection4Bound` / `..._of_red_le_blue_...` have been refactored to consume those graph-pair helpers instead of rebuilding the same manuscript loss-gap step inline.
- [x] Factor the branch-local mixed-case manuscript loss-gap step out at graph-pair level in `repo/Twobites/IndependentSets.lean`: the new wrappers `graphPair_paperRISILossGap_of_blueLeft_of_redRight_gapLower_of_card_le_of_two_le_gap_of_le` and `graphPair_paperRISILossGap_of_blueRight_of_redLeft_gapLower_of_card_le_of_two_le_gap_of_le` now expose the exact “image-card bound + two-gap lower bound + mixed deficit inequality” hypotheses for one embedding/graph pair, and the branch-specific near-one graph-pair helpers `graphPair_paperRISILossGap_of_paperNearOneReal_of_blue_le_red_of_two_div_le_loglog` / `..._of_red_le_blue_...` have been refactored through those generic wrappers. This pushes the remaining combinatorial RI collapse closer to the actual three-pool fixed-embedding task: prove the branch-local image-size and gap inequalities from the checked witness decomposition, then feed them into the direct deterministic-bounds manuscript shells.
- [ ] Finish the theorem-level existence shell above `lem:RI`: the RI case split is now packaged both as `paper_ri_exists_negCoeff_of_chooseOuterEventMass` and as the uniform fixed-coefficient endpoint `paper_ri_uniformCoeff_bound_of_chooseOuterEventMass`, the finite summation shell `finset_sum_le_of_card_mul_le` / `sum_powersetCard_le_of_choose_mul_le` now matches the manuscript’s last union-bound step over `k`-sets, the deterministic witness wrappers `NoSurvivingIndepSetCard`, `finalGraph_indepSetFree_of_noSurvivingIndepSetCard`, `triangleFreeRamseyWitness_of_noSurvivingIndepSetCard`, and `triangleFreeWithSmallIndepNum_of_noSurvivingIndepSetCard` now package the final-graph side of the paper statement, the paper-facing theorem shells `paperMainStatement_of_eventually_exists_finalGraphWitness` / `paperMain2WitnessStatement_of_eventually_exists_finalGraphWitness` isolate the exact global hypothesis shape, the finite unweighted sample-space skeleton `ConstructionData.sampleSpace` / `goodEventDSet` is now in place, and the weighted-event shell is now explicit via `constructionGraphBernoulliWeight`, `constructionEmbeddingUniformWeight`, `constructionProductWeight`, `paperConstructionWeight`, `constructionEventMass`, `goodSurvivingIndepSetEventSet`, `goodSurvivingIndepSetCardBadSet`, `constructionEventMass_goodSurvivingIndepSetCardBadSet_le_of_choose_mul_le`, `exists_goodEventD_with_noSurvivingIndepSetCard_of_mass_lt`, `exists_goodEventD_with_noSurvivingIndepSetCard_of_choose_mul_le_lt_goodMass`, `exists_goodEventD_with_noSurvivingIndepSetCard_of_paperChooseMulLe_lt_goodMass`, `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_lt_goodMass`, and the new explicit specialization `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_lt_explicitGoodMass`. The mass layer now also has exact normalization formulas `constructionGraphBernoulliWeight_sum_univ_eq`, `constructionEmbeddingUniformWeight_sum_univ_eq_one`, `constructionProductWeight_sum_univ_eq`, and `paperConstructionMass_sampleSpace_eq`, while the balanced-band witness side has exact lower-bound formulas `constructionGraphBernoulliWeight_bot_eq`, `constructionProductWeight_emptyBalancedConstructionData_eq`, `paperConstructionWeight_emptyBalancedConstructionData_eq`, and `paperConstructionMass_goodEventDSet_ge_emptyBalancedConstructionWeight_explicit`. The new decomposition theorems `constructionEventMass_eq_sum_embeddings_graphWeights`, `paperGoodSurvivingGraphPairMass`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_eq_sum_by_embedding` now isolate the first-stage/embedding split for one fixed `I`, the bridge lemmas `pairImage_card_eq_card`, `pairImage_mem_powersetCard`, `paperRIOuterRedImage_pairImage_eq_redImage`, `paperRIOuterBlueImage_pairImage_eq_blueImage`, `pairImage_mem_paperRIOuterEventSet`, `constructionEmbeddingUniformWeight_le_inv_choose_pairImage_card`, `inv_choose_le_paperRIOuterEventMass_of_mem`, `constructionEmbeddingUniformWeight_le_paperRIOuterEventMass_of_pairImage`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul`, `constructionEmbeddingUniformWeight_mul_constructionProductWeight_le_outerMass_mul_actualMass_of_baseOpenPairSet_card`, `goodSurvivingGraphPairActualMassIntegrand`, `goodSurvivingGraphPairProjectionChoiceMassBound`, `goodSurvivingGraphPairActualMassIntegrand_le_projectionChoiceMassBound`, and `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_projectionChoiceMass` now package the exact outer-event side together with a reduction of each fixed embedding all the way to an outer RI factor times a finite sum of literal `section4ProjectionChoiceMassSum` terms, and the new projection-choice exponential wrappers `section4ProjectionChoiceMassSum_section4F_le_exp_of_bounds`, `section4ProjectionChoiceMassSum_section4F_le_exp_of_totalError`, and `section4ProjectionChoiceMassSum_section4F_le_exp_of_LMS_totalError` mean the Section 4 exponent shell is now available directly at this reduced layer. The exact/monotone graph-constraint formulas `constructionGraphConstraintMass_eq` / `constructionGraphConstraintMass_le` are also now supplemented by the singleton helper layer `constructionGraphConstraintMass_nonneg`, `constructionGraphBernoulliWeight_le_constructionGraphConstraintMass_of_subset_disjoint`, `constructionProductWeight_le_constraintMass_mul_constraintMass`, the open-pair/nonedge comparison lemmas `redBaseOpenPairSet_image_sym2_disjoint_constructionGraphEdgeFinset`, `blueBaseOpenPairSet_image_sym2_disjoint_constructionGraphEdgeFinset`, `redBaseOpenPairSet_card_le_nonedgeCount`, `blueBaseOpenPairSet_card_le_nonedgeCount`, `baseOpenPairSet_card_le_nonedgeCountSum`, `constructionGraphBernoulliWeight_mul_le_section4ActualConditionedEventMass_of_baseOpenPairSet_card`, and the actual-conditioned-edge inclusions `section4URedCondPairSet_image_sym2_subset_constructionGraphEdgeFinset` / `section4UBlueCondPairSet_image_sym2_subset_constructionGraphEdgeFinset`. The local witness split `closedPairOn_of_redMixedDeletionWitness_of_mem`, `closedPairOn_of_blueMixedDeletionWitness_of_mem`, `sym2_mk_mem_redOppositeWitnessBiUnion_of_redMixedDeletionWitness_of_not_mem`, `sym2_mk_mem_blueOppositeWitnessBiUnion_of_blueMixedDeletionWitness_of_not_mem`, `redLift_edge_indep_trichotomy`, and `blueLift_edge_indep_trichotomy` is now packaged all the way into the concrete three-pool cover/cardinality lemmas `redProjectionPairBiUnion`, `blueProjectionPairBiUnion`, `redBaseClosedPairSet_image_sym2_subset_redMonochromaticWitnessBiUnion_union_redProjectionPairBiUnion_union_redOppositeWitnessBiUnion_of_indep`, `blueBaseClosedPairSet_image_sym2_subset_blueMonochromaticWitnessBiUnion_union_blueProjectionPairBiUnion_union_blueOppositeWitnessBiUnion_of_indep`, `redBaseClosedPairSet_card_le_redProjectionPairCount_filter_isRed_add_A_add_opposite_of_indep`, and `blueBaseClosedPairSet_card_le_blueProjectionPairCount_filter_isBlue_add_A_add_opposite_of_indep`. The remaining blocker is therefore no longer weighted sample-space plumbing, outer-count algebra, or the raw actual-conditioned-event shell, but the next quantitative Section 4 step: collapse the resulting finite sum of projection-choice masses using the checked three-pool witness decomposition for one fixed embedding, and then compare the resulting summed bad-event bound against the explicit `paperConstructionWeight` good-event lower bound.
- [ ] Finish the Section 3/4 paper target-gap endpoint in `repo/Twobites/IndependentSets.lean`: the canonical closed-pair bookkeeping now proves both target-gap orderings (`paperSection4OpenPairTargetNat_sub_errorSum_le_baseOpenPairSet_card_of_closedErrorBounds`, `paperSection4OpenPairTargetNat_sub_openError_le_baseOpenPairSet_card_of_closedErrorBounds`, and the swapped-color/real variants), and the natural closed-pair decomposition packages the LMS-plus-huge bounds as `redBaseClosedPairSet_card_le_paperHugeRedCrossTargetNat_add_of_indep_of_LMSHugeBounds`, `blueBaseClosedPairSet_card_le_paperHugeBlueCrossTargetNat_add_of_indep_of_LMSHugeBounds`, and the combined bridges `paperSection4OpenPairTargetNat_sub_openError_le_baseOpenPairSet_card_of_splitClosedErrorBounds` / `paperSection4OpenPairTarget_sub_openError_le_baseOpenPairSet_card_of_swappedClosedRealErrorBounds`. That bridge is now instantiated at paper deterministic scale by `paperSection4OpenPairTargetNat_sub_openError_le_baseOpenPairSet_card_of_uniformPaperErrorBounds`, `paperSection4OpenPairTargetNat_sub_openError_le_baseOpenPairSet_card_of_paperDeterministic`, and the sharpened manuscript-scale wrappers `paperSection4OpenPairTarget_sub_ten_mul_eps_mul_paperKSq_le_baseOpenPairSet_card_of_uniformPaperErrorBounds`, `paperSection4OpenPairTarget_sub_ten_mul_eps_mul_paperKSq_le_baseOpenPairSet_card_of_paperDeterministic`, and `paperSection4OpenPairTargetNat_sub_ceil_ten_mul_eps_mul_paperKSq_le_baseOpenPairSet_card_of_paperDeterministic`. The new loss-side package `cast_section4LossTarget_le_nine_mul_eps_mul_paperKSq_of_uniformBounds` and `section4LossTargetNat_le_ceil_nine_mul_eps_mul_paperKSq_of_paperDeterministic` now show that the literal `hLossLeTarget` term from Section 4 costs only `9 * ε1 * k^2` at manuscript scale, while `section4ActualConditionedEventMass_antitone`, `section4ActualConditionedEventMass_baseOpenPairSet_card_le_exp_of_indep_of_uniformError_of_paperLargeWitness_of_paperHugeWitness_of_targetGap`, `section4ActualConditionedEventMass_baseOpenPairSet_card_le_exp_of_indep_of_uniformError_of_paperLargeWitness_of_paperHugeWitness_of_paperDeterministicTargetGap`, `paper_risi_finiteMassBound_of_targetGapArithmetic`, and the lighter natural-loss wrapper `paper_risi_finiteMassBound_of_paperRISILossGap` reconnect the deterministic target gap to the literal `section4ActualConditionedEventMass` at `N = |baseOpenPairSet I|`. The new lower-bound lemmas `paperSection4OpenPairTarget_lower_bound_of_targetBounds`, `..._of_blueLeft_of_redLeft`, `..._of_blueLeft_of_redRight`, and `..._of_blueRight_of_redLeft` now expose exactly the three branch choices from the paper’s `f(ℓ_R,\ell_B)` case split, while `paperRISILossNat`, `paperRISILossNat_le_natCeil_nineteen_mul_eps_mul_paperKSq_add_one`, `paperSection4Branch_blueLeft_redLeft_eq`, `paperSection4Branch_blueLeft_redRight_eq`, `paperSection4Branch_blueRight_redLeft_eq`, `paper_risi_hLossGap_of_blueLeft_of_redLeft`, `paper_risi_hLossGap_of_blueLeft_of_redLeft_sumGap`, `paper_risi_hLossGap_of_blueLeft_of_redLeft_natGap`, `paper_risi_hLossGap_of_blueLeft_of_redRight`, `paper_risi_hLossGap_of_blueLeft_of_redRight_sumGap`, `paper_risi_hLossGap_of_blueLeft_of_redRight_natDeficit`, `paper_risi_hLossGap_of_blueLeft_of_redRight_subGap`, `paper_risi_hLossGap_of_blueLeft_of_redRight_subGap_of_card_le_of_one_le_gap_of_le`, `paper_risi_hLossGap_of_blueLeft_of_redRight_gapLower_of_card_le_of_one_le_gap_of_le`, `paper_risi_hLossGap_of_blueRight_of_redLeft`, `paper_risi_hLossGap_of_blueRight_of_redLeft_sumGap`, `paper_risi_hLossGap_of_blueRight_of_redLeft_natDeficit`, `paper_risi_hLossGap_of_blueRight_of_redLeft_subGap`, `paper_risi_hLossGap_of_blueRight_of_redLeft_subGap_of_card_le_of_one_le_gap_of_le`, and `paper_risi_hLossGap_of_blueRight_of_redLeft_gapLower_of_card_le_of_one_le_gap_of_le` now package the loss term and all three branch formulas as nat-valued `hLossGap` inputs for the final finite-mass endpoint; the large-sum branch is reduced to a natural sum-gap times `(k-1)`, and the two mixed branches are reduced to a natural shortfall-versus-right-gap inequality together with the one-gap cap arithmetic that upgrades image-card parameter bounds and a separately proved lower bound on the right gap into the exact mixed-branch hypotheses. The remaining gap is now the actual paper-parameter arithmetic showing that the near-one regimes at `β = 1/2` and `κ = 1 + ε` supply those mixed shortfall/right-gap inequalities, followed by the last `lem:RI` asymptotic summation.
- [ ] Use the new direct right-gap bridges in `repo/Twobites/IndependentSets.lean` to cut out the last mixed-branch bookkeeping: `paperKNat_le_paperKNat_sub_redImage_card_sub_paperCapNat_of_card_le_of_two_le_gap_of_le`, `paperKNat_le_paperKNat_sub_blueImage_card_sub_paperCapNat_of_card_le_of_two_le_gap_of_le`, `paper_risi_hLossGap_of_blueLeft_of_redRight_gapLower_of_card_le_of_two_le_gap_of_le`, and `paper_risi_hLossGap_of_blueRight_of_redLeft_gapLower_of_card_le_of_two_le_gap_of_le` now reduce the near-one branches to proving paper-parameter lower bounds of the form `paperKNat δ ≤ paperKNat (1 + ε) - |π_*(I)| - paperCapNat (1/2) ε2` under the manuscript’s `x_R + x_B` hypotheses.
- [ ] Finish the manuscript-coefficient near-one wrappers in `repo/Twobites/IndependentSets.lean`: besides `paper_risi_hLossGap_of_blueLeft_of_redRight_gapLower_of_paperNearOne` / `paper_risi_hLossGap_of_blueRight_of_redLeft_gapLower_of_paperNearOne` and their large-`n` lifts `..._of_two_div_le_loglog`, the new coefficient-halving lemmas `blueImage_card_le_paperKNat_half_of_sum_le_of_blue_le_red`, `redImage_card_le_paperKNat_half_of_sum_le_of_red_le_blue`, the nat-real bridge `imageSum_card_le_paperKNat_of_paperNearOneReal`, the paper-specific image bounds `blueImage_card_le_paperKNat_of_paperNearOneSum_of_blue_le_red` / `redImage_card_le_paperKNat_of_paperNearOneSum_of_red_le_blue` and `..._of_paperNearOneReal_...`, and the lifted mixed wrappers `paper_risi_hLossGap_of_blueLeft_of_redRight_gapLower_of_paperNearOneSum_of_blue_le_red_of_two_div_le_loglog`, `paper_risi_hLossGap_of_blueRight_of_redLeft_gapLower_of_paperNearOneSum_of_red_le_blue_of_two_div_le_loglog`, `paper_risi_hLossGap_of_blueLeft_of_redRight_gapLower_of_paperNearOneReal_of_blue_le_red_of_two_div_le_loglog`, and `paper_risi_hLossGap_of_blueRight_of_redLeft_gapLower_of_paperNearOneReal_of_red_le_blue_of_two_div_le_loglog` now reduce the near-one cases to the manuscript’s native real sum hypothesis `(|π_R(I)| + |π_B(I)| : ℝ) ≤ (1 + ε / 2) * k`, the symmetry choice `x_B ≤ x_R` (or its red-symmetric analogue), and the corresponding loss inequality.
- [ ] Use the new large-`n` bridge `two_le_paperK_of_two_div_le_of_le_one` from `repo/Twobites/ParameterBounds.lean`, the paper-near-one real/sum/card wrappers in `repo/Twobites/IndependentSets.lean`, and the new checked coefficient lemmas `paperRI_nearOne_mixedCoeff_eq`, `paperRI_nearOne_mixedCoeff_le_final`, and `paperRI_nearOne_finalCoeff_neg` to finish the manuscript’s middle `lem:RI` case at the combined probability-plus-RISI exponent level. The remaining work is no longer auxiliary gap bookkeeping: the mixed branches must now be packaged with the outer `P(\mathcal S_{I,\ell_R,\ell_B})` exponent, since the raw Section 4 target alone can be negative near the lower edge of the near-one regime.
- [ ] Finish the final `lem:RI` / theorem-level union-bound shell in `repo/Twobites/IndependentSets.lean`: the union-factor outer layer is now packaged by `paperRI_choosePaperKNat_le_exp`, `paperRIChooseOuterEventMass_le_exp_of_paperParameterLogs`, the cleaned exponent lemmas `paperRI_chooseOuterExp_eq` / `paperRI_chooseOuterExp_le_mainRemainder`, the new large-`n` domination helpers `loglog_le_half_eta_mul_log_of_two_le_loglog_of_two_div_le` / `one_le_half_eta_mul_log_of_two_div_loglog_le`, the `paperKNat`/`paperK` bridges `paperKNat_le_two_mul_paperK_of_one_le`, `two_le_delta_mul_paperK_one_add_eps_of_two_div_le`, the outer-budget packages `paperRI_chooseOuterExp_le_largeSum_main_plus_budget`, `paperRI_chooseOuterExp_le_nearOne_main_plus_budget`, `paperRI_chooseOuterExp_add_risiLargeSum_le_htotal`, and `paperRI_chooseOuterExp_add_risiNearOne_le_htotal`, and the end-to-end specializations `paper_ri_eqLong_bound_smallSum_of_chooseOuterEventMass_le_exp_of_mainRemainder_of_images`, `paper_ri_eqLong_bound_largeSum_of_chooseOuterEventMass_le_exp_of_mainRemainder_of_images_of_budget`, `paper_ri_eqLong_bound_largeSum_of_chooseOuterEventMass_le_exp_of_mainRemainder_of_images_of_section_le`, `paper_ri_eqLong_bound_nearOne_of_chooseOuterEventMass_le_exp_of_mainRemainder_of_images_of_section_le`, `paper_ri_eqLong_bound_nearOne_of_chooseOuterEventMass_le_exp_of_mainRemainder_of_images_of_budget_of_blueCapNat`, `paper_ri_eqLong_bound_nearOne_of_chooseOuterEventMass_le_exp_of_mainRemainder_of_images_of_budget`, and the new case-split dispatcher `paper_ri_exists_negCoeff_of_chooseOuterEventMass`. The RI regime endpoints are now dispatched by one top-level theorem; the remaining gap is the actual finite union-bound / positive-probability wrapper that sums over all `k`-sets and converts the resulting RI estimate into the existence theorem behind `paperMainStatement`.
- [ ] Package the outer `eq:long` exponent itself in `repo/Twobites/ParameterBounds.lean` / `repo/Twobites/IndependentSets.lean`: the new checked small/large-sum coefficient lemmas `paperRI_smallSumCoeff_le`, `paperRI_smallSumCoeff_neg`, `paperRI_largeSumCoeff_eq`, `paperRI_largeSumCoeff_le_final`, and `paperRI_largeSum_finalCoeff_neg` now settle the non-middle regimes algebraically, so the remaining `lem:RI` work is to build one paper-scale combined-exponent wrapper and then feed the small, middle, and large regime coefficients into the final union-bound summation.
- [ ] Use the new coefficient- and exponent-sign wrappers `paperRI_eqLongCoeff_neg_of_smallSum`, `paperRI_eqLongCoeff_neg_of_largeSum`, `paperRI_eqLongCoeff_neg_of_nearOne`, `paperRI_eqLongExp_neg_of_smallSum`, `paperRI_eqLongExp_neg_of_largeSum`, and `paperRI_eqLongExp_neg_of_nearOne` in `repo/Twobites/ParameterBounds.lean` to close the three `lem:RI` regimes uniformly once the outer `P(\\mathcal S_{I,\\ell_R,\\ell_B})` term has been packaged. At this point the remaining gap is no longer branch arithmetic, but the absence of a single paper-scale wrapper around the actual `eq:long` exponent and the final asymptotic union-bound sum.
- [ ] Use the new outer-event interface wrappers in `repo/Twobites/IndependentSets.lean` to connect the exact family `paperRIOuterEventMass (paperMNat n) |π_R(I)| |π_B(I)| (paperKNat (1 + ε) n)` to the already formalized `eq:long` bridges: `paper_ri_outerEventMass_le_exp_of_paperParameterLogs_of_images` now turns the actual image-size hypotheses on `I` into an exponential bound via `paperRIOuterEventMass_le_exp_of_paperParameterLogs`, and `paper_ri_eqLong_bound_of_outerEventMass_le_exp_of_paperParameterLogs_of_le` specializes the abstract product theorem `paper_ri_eqLong_bound_of_outer_le_exp_of_paperRISILossGap_of_le` to that exact outer mass. The remaining `lem:RI` gap is now the total-exponent comparison that inserts the final `{n \\choose k}` union factor and simplifies the combined outer-plus-RISI exponent in the small, large, and near-one regimes.
- [ ] Use the new endpoint cleanups in `repo/Twobites/IndependentSets.lean` to finish the remaining `eq:long` regimes without reopening the Bernoulli machinery: `section4BernoulliMass_le_one` and `section4ActualConditionedEventMass_le_one` package the trivial small-branch bound, `paper_ri_eqLong_bound_of_outer_smallSum` closes that branch directly from the checked small-sum coefficient sign, and the new cleaned small-branch wrapper `paper_ri_eqLong_bound_smallSum_of_chooseOuterEventMass_le_exp_of_mainRemainder_of_images` now applies the outer-event side without reopening the abstract `eq:long` theorem. The remaining large/near-one post-composition wrappers `paper_ri_eqLong_bound_largeSum_of_le` and `paper_ri_eqLong_bound_nearOne_of_le` still need the total-exponent comparison in hand. On the outer counting side, the exact finite family `paperRIOuterEventSet`, its witness overcount `paperRIOuterWitnessSet`, the card/mass bounds `paperRIOuterEventSet_card_le_choose_choose_choose` and `paperRIOuterEventMass_le_outerCombBound`, the simplified ratio wrapper `paperRIOuterEventMass_le_choose_choose_mul_pow_ratio`, the exponential interface `paperRIOuterPowRatioBound` with `paperRIOuterEventMass_le_powRatioBound`, `paperRIOuterEventMass_le_exp_log_powRatioBound`, and `paperRIOuterEventMass_le_exp_of_log_powRatioBound_le`, the exact logarithmic normalization `paperRIOuterPowRatioBound_log_eq`, the separated wrapper `paperRIOuterEventMass_le_exp_of_chooseLogs_le`, the paper-style choose estimates `choose_cast_le_exp_mul_div_pow` and `paperRI_logChoose_le`, the bundled outer wrappers `paperRIOuterEventMass_le_exp_of_logChooseFormula_le` / `paperRIOuterEventMass_le_exp_of_logChooseRatioFormula_le`, the scaled-variable reductions `paperRI_outerLogChooseRatioFormula_eq_scaled`, `paperRIOuterEventMass_le_exp_of_scaledLogs`, and `paperRIOuterEventMass_le_exp_of_scaledLogs_one`, the new `paperMNat` bridge lemmas `paperSNat_eq_ceil_of_pos`, `div_paperSNat_sub_one_lt_paperMNat`, `four_le_paperM_of_two_le_loglog`, and `paperM_log_sub_log_four_le_log_paperMNat_of_two_le_loglog`, and the paper-parameter outer wrapper `paperRIOuterEventMass_le_exp_of_paperParameterLogs` now reduce the first lines of `lem:RI` to one remaining paper-scale task: feed that outer exponential theorem through the large- and near-one `eq:long` wrappers, then finish the final `{n \choose k}` union-bound factor and summation.
- [ ] Continue the Section 4 deterministic reveal lemmas in `repo/Twobites/IndependentSets.lean`: the current infrastructure now proves the total `F_1` size bounds `log n * |F_1| ≤ 2|I|` and `|F_1| ≤ 2|I| / log n`, the easy inclusion `F₂ ⊆ L_I ∪ H_I`, the generic hard inclusion `baseImage ∩ H_I ⊆ F₂`, and its paper-scale specialization via `paperSection4Bound_le_paperT1_of_two_le_loglog_of_fiberScale`; there are also paper wrappers `section4F2_card_le_card_LPart_union_HPart_of_paper`, `..._add_card_HPart_of_paper`, the combined Section 4 vertex-budget bounds `section4F1_union_section4F2_card_le_two_mul_card_div_log_add_card_LPart_add_card_HPart` and `card_mul_section4F1_union_section4F2_card_le_two_mul_card_sq_div_log_add_card_mul_parts`, the direct-cardinality reveal wrappers `cast_section4RevealBudget_le_eps_mul_paperKSq_of_cardBounds`, `HPart_card_le_paperHugeWitnessNat_of_goodEventD`, and `cast_section4RevealBudget_le_eps_mul_paperKSq_of_LBound_of_paperHugeWitness`, the precise base-open-pair reveal bookkeeping objects `revealedBaseOpenPairSet` / `unrevealedBaseOpenPairSet` together with the orientation/injection control into `section4RevealPairSet`, the equalities `revealedBaseOpenPairSet_section4F_eq_union` / `unrevealedBaseOpenPairSet_section4F_eq_union` showing that `F_0` does not touch base-image open pairs, and the subtraction lemma `openPair_lower_bound_sub_section4_budget_le_unrevealed_section4F`. The paper’s actual unrevealed same-color pair object is now also formalized as `basePairSet` / `unrevealedBasePairSet`, and `openPair_lower_bound_sub_section4_budget_le_unrevealedBasePairSet_section4F` transfers the deterministic lower bound from unrevealed open pairs to unrevealed same-color pairs in `E_I`. The later `T_R` / `T_B` survivors are now formalized too via `redBaseClosedPlusPair` / `blueBaseClosedPlusPair`, `section4TRedPairSet` / `section4TBluePairSet` / `section4TPairSet`, and the generic subtraction wrappers `unrevealedBasePair_lower_bound_sub_sameColorClosedPlus_le_section4TPairSet` and `openPair_lower_bound_sub_section4_budget_sub_sameColorClosedPlus_le_section4TPairSet`. The same-color removal term is now bounded concretely by `redBaseClosedPlusPairSet_card_le_redProjectionPairCount`, `blueBaseClosedPlusPairSet_card_le_blueProjectionPairCount`, `sameColorClosedPlusBasePairSet_card_le_projectionPairCount_sum`, and the specialized endpoint `openPair_lower_bound_sub_section4_budget_sub_projectionPairCount_sum_le_section4TPairSet`; the opposite-color witness side is now also formalized via `redOppositeWitnessBiUnion` / `blueOppositeWitnessBiUnion`, the conditioned finsets `section4URedCondPairSet` / `section4UBlueCondPairSet`, the combined/event-counting objects `section4UCondPairSet` / `section4TRemainingPairSet`, and the exact cardinality bridges `section4TRemainingPairSet_card_eq_of_card_eq_condCounts`, `section4TPairSet_lower_bound_sub_oppositeProjectionCounts_of_indep_le_card_sub_uR_sub_uB`, and `openPair_lower_bound_sub_section4_budget_sub_projectionPairCount_sum_sub_oppositeProjectionCounts_le_card_sub_uR_sub_uB`. The finite counted-event wrapper is now explicit end-to-end: `section4URedChoiceSet` / `section4UBlueChoiceSet` / `section4UChoiceSet` enumerate candidate `U_R,U_B` witness sets of prescribed sizes, the literal second-stage sample space `section4TRedChoiceSet` / `section4TBlueChoiceSet` / `section4TChoiceSet` packages all size-`(u_R,u_B)` edge outcomes inside `T_R × T_B`, `section4SecondStageEventMass` is the generic Bernoulli mass on such finite event families, and `section4UCondChoiceOutcome_mem_section4TChoiceSet_of_card_eq` / `section4UCondChoiceEvent_subset_section4TChoiceSet_of_card_eq` place the actual conditioned outcome/event inside that sample space. The exact-count lemmas `section4UCondChoiceEventMass_eq_bernoulliMass_of_card_eq`, `section4UCondChoiceEventMass_eq_zero_of_not_card_eq`, `section4UCondChoiceEventMassSum_eq_section4UCondChoiceEventMass_of_card_le`, and `section4UCondChoiceEventMassSum_eq_bernoulliMass_of_card_le` collapse the summed counted-event family back to the single actual conditioned outcome whenever the index caps cover the true `(u_R,u_B)` counts, `section4SecondStageLossNat` now names the aggregate reveal-plus-witness loss term subtracted from a base open-pair lower bound, `section4ActualConditionedEventMass` names the literal finite mass of the actual conditioned second-stage event on `section4F`, `section4UCondChoiceEventMass_section4F_le_exp_of_indep_of_bounds` transfers the checked exponential estimate straight to the literal actual conditioned Section 4 event mass on `section4F`, `section4UCondChoiceEventMass_section4F_le_exp_of_indep_of_bounds_of_sum_le` trades the separate red/blue witness caps for a single exponent error term, `section4UCondChoiceEventMass_section4F_le_exp_of_indep_of_totalError` packages the whole checked reveal-plus-witness loss into one total-error exponent against a chosen base open-pair lower bound, and `section4ActualConditionedEventMass_le_exp_of_indep_of_totalError` is the current semantic endpoint for the actual conditioned event. The new structural loss package `outside_section4F_subset_LPart_union_MPart_union_SPart_of_HPart_subset_section4F2`, `oppositeProjectionPairCount_sum_outside_section4F_le_partPairCount_LMS_of_HPart_subset_section4F2`, `projectionPairCount_sum_baseImage_le_partPairCount_LMS_add_huge_of_thresholds`, `section4SecondStageLossNat_le_revealBudget_add_two_mul_partPairCount_LMS_add_huge`, `section4SecondStageLossNat_add_witnessCaps_le_revealBudget_add_three_mul_partPairCount_LMS_add_huge`, `cast_partPairCount_LMS_le_sum_of_thresholds`, `section4ActualConditionedEventMass_le_exp_of_indep_of_LMS_totalError`, `section4ActualConditionedEventMass_le_exp_of_indep_of_splitPartTotalError`, and the paper-facing specializations `paper_small_deterministic`, `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformPartError`, `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformError`, `cast_section4RevealBudget_le_eps_mul_paperKSq_of_goodEventD_of_witnessBounds`, `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformError_of_revealWitnessBounds`, `paperSection4OpenPairTargetNat` / `paperSection4OpenPairTarget`, and the new wrappers `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformError_of_revealLWitness_of_paperHugeWitness`, `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformError_of_revealLBound_of_paperHugeWitness`, `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformError_of_revealWitnessBounds_of_targetGap`, `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformError_of_revealLBound_of_paperHugeWitness_of_targetGap`, and `section4ActualConditionedEventMass_le_exp_of_indep_of_uniformError_of_revealLWitness_of_paperHugeWitness_of_targetGap` now reduce the remaining reveal frontier to the final paper arithmetic: prove a concrete direct card bound on `|L_I|`, prove the deterministic open-pair lower bound into `paperSection4OpenPairTargetNat I κ cap - openError`, and wrap the resulting estimate as the genuine conditional probability statement of `lem:RISI`, followed by the last asymptotic simplification for `lem:RI`. The viable huge-case endpoint remains `paper_huge_deterministic_of_paperHugeWitness_of_eps1Slack_of_three_of_diagScale_of_codegScale`; the later collapsed specializations `...of_split`, `...of_doubleEps`, and `...of_kSmall` are ruled out by the new obstruction lemmas.
- [x] Finish rerouting the huge-case proof through the direct witness-error path in `repo/Twobites/IndependentSets.lean`: the canonical wrappers `paper_huge_deterministic_of_witnessErrorBounds_of_realErrorBound`, `..._of_coeffBound`, `..._of_rightSmall`, and `..._of_paramDeficit` now all run through the split-right package (`paperHuge*CrossWitnessRightSplitErrorProp`, `paperHuge*CrossConcreteBoundNat_le_target_of_split_right_error_bounds`, `paper_huge_deterministic_of_witnessSplitRightErrorBounds`), and the old monolithic right-smallness target is no longer used in that chain. The remaining huge-case frontier is the large-`n` arithmetic needed to prove the explicit split right-branch coefficient inequalities now exposed in those wrappers.
- [x] Discharge the split right-branch coefficient hypotheses at the paper scale in `repo/Twobites/ParameterBounds.lean` and thread them through the huge wrappers in `repo/Twobites/IndependentSets.lean`: the new lemmas `splitRightCoeff_le_eps_mul_choose_two_of_sum_le_of_three_mul_le` and `splitRightCoeff_le_eps_mul_cap_choose_two_add_choose_two_of_sum_le_of_three_mul_le` now prove the general `B`-level arithmetic, and `paper_huge_deterministic_of_witnessErrorBounds_of_coeffBound_of_rightSmall` / `..._of_paramDeficit` / `..._of_paramDeficit_of_split` / `..._of_paramDeficit_of_three` derive their `hblueCrossRight` / `hredCrossRight` obligations directly from that split package.
- [ ] Connect the new `finalGraph.CliqueFree 3` theorem and the part-restricted closed-pair bookkeeping to the eventual independence-number proofs in `repo/PaperTheorems.lean`.

## Completed
- [x] Prove the first deterministic reveal consequences in `repo/Twobites/IndependentSets.lean`: same-color projection images now identify with `baseNeighborSet ∩ baseImage`, `X_(inl r)(I)` / `X_(inr b)(I)` are rewritten as disjoint unions of same-color fibers, `section4F1_card_mul_log_le_two_mul_card` gives the paper's `|F_1|` bound, and `section4F2_subset_LPart_union_HPart_of_log_pos_of_paperT3_le_paperT2` packages the easy `F₂ ⊆ L_I ∪ H_I` direction.
- [x] Start the Section 4 language in `repo/Twobites/IndependentSets.lean`: `baseImage`, `baseFiber`, `baseNeighborSet`, the reveal-process sets `section4F0` / `section4F1` / `section4F2` / `section4F`, and the localized open-pair predicates `OpenPairOn` / `OpenPairPlusOn` now package the paper’s “revealed versus unrevealed” bookkeeping, with basic membership, monotonicity, and `Finset.univ` comparison lemmas already checked.
- [x] Record the final obstruction above the paperHugeWitness huge-chain in `repo/Twobites/ParameterBounds.lean`: `three_loglog_codegCoeff_nonneg`, `not_three_mul_paperK_three_loglog_codegCoeff_le_cross_residual`, and `not_three_mul_paperK_two_mul_le_cross_residual` show that the surviving collapsed wrappers `...of_split` / `...of_doubleEps` / `...of_kSmall` force impossible right-smallness goals, so the usable huge-case endpoint is the explicit-`δsplit` theorem `paper_huge_deterministic_of_paperHugeWitness_of_eps1Slack_of_three_of_diagScale_of_codegScale`.
- [x] Turn the old `t_2 ≤ t_1` reduction step into actual large-`n` arithmetic in `repo/Twobites/ParameterBounds.lean`: `loglog_pos_of_two_div_le`, `one_le_loglog_of_two_div_le_of_le_one`, `loglog_le_sqrt_log_of_two_le_loglog`, `paperT2_le_paperT1_of_two_le_loglog`, and `ceil_paperT2_le_ceil_paperT1_of_two_le_loglog` now remove the remaining analytic hypothesis from the threshold comparison, and the later huge-case wrappers in `repo/Twobites/IndependentSets.lean` reuse the new `log log n` helper lemmas instead of reproving those facts ad hoc.
- [x] Prove and consume the large-`n` lower bound `2 < t_1` in the paperHugeWitness route: `two_le_loglog_of_two_div_le_of_le_one`, `two_lt_paperT1_of_two_le_loglog`, and `two_lt_paperT1_of_two_div_le_of_le_one` in `repo/Twobites/ParameterBounds.lean` now derive the witness threshold directly from the standard `2 / ε₁ ≤ log log n` hypothesis, and the later `(2 + ε₁)` huge-witness wrappers in `repo/Twobites/IndependentSets.lean` no longer take `hT1 : 2 < paperT1 n` as an explicit assumption.
- [x] Collapse the huge split-error arithmetic to a single coefficient: `paper_huge_deterministic_of_split_degree_le_paperPM_of_codegree_le_of_small_coeff` in `repo/Twobites/IndependentSets.lean` now combines the packaged degree/codegree contributions via `paperK_add` and `paperK_le_paperK_of_le`, reducing the remaining huge-case work to one split coefficient `δsplit`, two right-branch smallness inequalities, and the final coefficient comparisons.
- [x] Package the huge-case split coefficient and the `δ_sumGap` absorption one step further: `nonneg_of_one_le_paperK` and `splitCoeff_nonneg` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_split_degree_le_paperPM_of_codegree_le_of_small_coeff_of_gap` in `repo/Twobites/IndependentSets.lean`, now eliminate the intermediate `δ_err` parameter so the remaining huge-case arithmetic is stated directly as one split coefficient bound, two right-branch smallness inequalities, and the final comparisons `ρ + (1+ε₂)β + δsplit + δ_sumGap + δ_gap ≤ κ`.
- [x] Package the diagonal huge terms from the same `p m`-scale degree estimate: `paper_huge_deterministic_of_small_coeff_gap_diag` in `repo/Twobites/IndependentSets.lean` now derives both diagonal huge hypotheses from a single paper-scale bound on `((paperKNat κ)/2) * paperK (((bound:ℝ) * β_deg) / s)`, so the remaining huge-case frontier is purely coefficient arithmetic plus the right-branch smallness estimates.
- [x] Remove the last ceiling from the diagonal huge hypothesis: `paper_huge_deterministic_of_small_coeff_gap_diag_paperK` in `repo/Twobites/IndependentSets.lean` uses `paperKNat κ ≤ paperK κ + 1` to replace the diagonal bound `((paperKNat κ)/2) * paperK (...)` by the cleaner parameter-only form `((paperK κ + 1)/2) * paperK (...)`.
- [x] Replace the natural-number split coefficient by a real upper bound: `cast_choose_two_le_sq_div_two` and `splitCoeff_le_of_bound_le` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_small_coeff_gap_diag_paperK_of_bound_le` in `repo/Twobites/IndependentSets.lean`, now convert the awkward term `((bound.choose 2 : ℕ) : ℝ)` into `B^2/2` once `(bound:ℝ) ≤ B`.
- [x] Remove the extra natural-number bound parameter from the huge-case reduction: `paper_huge_deterministic_of_small_coeff_gap_diag_paperK_of_witness_bound_le` in `repo/Twobites/IndependentSets.lean` specializes the previous theorem to `bound = witnessSize`, so the remaining huge-case arithmetic is phrased directly in terms of one real witness-size bound `B`.
- [x] Repackage the diagonal huge hypothesis as a coefficient inequality: `half_add_one_mul_paperK_le_eps_mul_paperKSq_of_le` in `repo/Twobites/ParameterBounds.lean` and `paper_huge_deterministic_of_small_coeff_gap_diag_paperK_of_witness_bound_le_of_diagCoeff` in `repo/Twobites/IndependentSets.lean` replace the remaining diagonal paper-scale bound by the simpler coefficient condition `(B * β_deg / s) * (paperK κ n + 1) ≤ 2 * ε₁ * κ * paperK κ n`.
- [x] Eliminate the separate nonnegativity side conditions on `β_deg` and `q_codeg`: `nonneg_of_natCast_le` and `nonneg_of_le_paperP_mul_paperM` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_witness_bound_of_diagCoeff_of_bounds` in `repo/Twobites/IndependentSets.lean`, now derive those signs directly from the projected degree/codegree bounds supplied by `𝒟`.
- [x] Absorb the auxiliary diagonal-gap parameters into the final huge-case gap hypotheses: `paper_huge_deterministic_of_witness_bound_of_diagCoeff_of_bounds_of_gaps` in `repo/Twobites/IndependentSets.lean` now derives the earlier `δ_R, δ_B` assumptions from the stronger comparisons involving `δsplit + δsumGap + δgapR/B`, so the remaining huge-case frontier matches the paper's final coefficient/gap inequalities more closely.
- [x] Add the exact `k/t_1 = \kappa \log\log n` algebra needed by the Section 3 `|H_I|` estimate: `paperT1_mul_loglog_eq_sqrt`, `paperK_eq_mul_loglog_mul_paperT1`, and `paperK_div_paperT1_eq_mul_loglog` in `repo/Twobites/ParameterBounds.lean` now convert witness-size bounds stated in units of `t_1` into the paper's natural `\kappa \log\log n` scale.
- [x] Package the huge-case theorem at the paper's natural witness scale: `le_mul_mul_loglog_of_le_mul_paperK_div_paperT1` in `repo/Twobites/ParameterBounds.lean` and `paper_huge_deterministic_of_ratio_witness_bound_of_diagCoeff_of_bounds_of_gaps` in `repo/Twobites/IndependentSets.lean` now let the final huge-case argument start from a witness-size bound of the form `B * (paperK κ n / paperT1 n)`, automatically rewritten as `B * \kappa \log\log n`.
- [x] Expose the huge-part cardinality estimate at the same scale as the final huge-case theorem: `HPart_card_lt_mul_paperK_div_paperT1_of_goodEventD_of_lt_of_le` and `HPart_card_lt_mul_mul_loglog_of_goodEventD_of_lt_of_le` in `repo/Twobites/IndependentSets.lean` now turn the Section 3 witness inequality into explicit bounds on `|H_I|` in the ratio form `B * (paperK κ n / paperT1 n)` and the paper form `B * \kappa \log\log n`.
- [x] Split the huge-part witness inequality into the paper's two arithmetic ingredients: `paperKNat_lt_mul_ceil_paperT1_sub_choose_mul_of_two_mul_lt` in `repo/Twobites/ParameterBounds.lean` and the corresponding `HPart_card_lt_mul_paperK_div_paperT1_of_goodEventD_of_two_mul_lt_of_choose_le` / loglog variant in `repo/Twobites/IndependentSets.lean` now reduce the `H_I` cardinality bound to proving `2k < w t_1` and a separate choose-error bound.
- [x] Package the first concrete huge witness `w = ceil(2k/t_1) + 1`: `paperHugeWitnessNat`, `paperHugeWitnessNat_le_two_mul_paperK_div_paperT1_add_two`, and `two_mul_paperKNat_lt_paperHugeWitnessNat_mul_ceil_paperT1` in `repo/Twobites/ParameterBounds.lean`, together with `HPart_card_lt_two_mul_paperK_div_paperT1_add_two_of_goodEventD_of_paperHugeWitness` in `repo/Twobites/IndependentSets.lean`, now give a checked `|H_I| < 2(k/t_1) + 2` bound once the choose-error term is controlled.
- [x] Lift the canonical huge witness to the paper's loglog scale and feed it into the huge-case theorem: `paperHugeWitnessNat_le_two_mul_mul_loglog_add_two` and `paperHugeWitnessNat_splitCoeff_le_of_loglog` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_paperHugeWitness_of_diagCoeff_of_bounds_of_gaps` in `repo/Twobites/IndependentSets.lean`, now reduce the huge-case frontier to the remaining `+2`-perturbed coefficient inequalities at the explicit scale `2 \kappa \log\log n + 2`.
- [x] Absorb the canonical witness `+2` into a small multiplicative slack on the paper scale: `paperHugeWitnessNat_le_two_add_eta_mul_paperK_div_paperT1` and `paperHugeWitnessNat_le_two_add_eta_mul_mul_loglog` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_paperHugeWitness_of_ratioGap_of_diagCoeff_of_bounds_of_gaps` in `repo/Twobites/IndependentSets.lean`, now reduce the huge-case frontier to choosing an asymptotic slack `η` and proving the corresponding `(2 + η)` coefficient inequalities.
- [x] Supply the canonical-witness slack from a standard large-`n` loglog hypothesis: `two_le_mul_of_two_div_le`, `two_le_eta_mul_mul_loglog_of_two_div_loglog_le`, and `two_le_eta_mul_paperK_div_paperT1_of_two_div_loglog_le` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_paperHugeWitness_of_loglogSlack_of_diagCoeff_of_bounds_of_gaps` in `repo/Twobites/IndependentSets.lean`, now turn the witness-gap input into the familiar asymptotic condition `2 / η ≤ log log n`.
- [x] Rule out the latest over-packaged huge-case smallness target: `cross_residual_sub_one_le_paperK` and `not_three_mul_paperK_add_nonneg_le_cross_residual` in `repo/Twobites/ParameterBounds.lean` show that the current specialization `3 * paperK (ε₁ κ + δ_codeg) n ≤ ε₁ * (deficit - 1)` cannot hold once `ε₁ > 0` and `δ_codeg ≥ 0`, so the huge-case refactor must keep the right-branch smallness on the true `o(k)` witness-error term instead of the combined split coefficient.
- [x] Specialize the canonical-witness slack to the paper's own error parameter: `paper_huge_deterministic_of_paperHugeWitness_of_eps1Slack_of_diagCoeff_of_bounds_of_gaps` in `repo/Twobites/IndependentSets.lean` now packages Lemma `lem:huge` directly at the explicit `(2 + ε₁)` coefficient scale under the standard large-`n` hypothesis `2 / ε₁ ≤ log log n`.
- [x] Add monotonicity in the huge-case coefficient scale: `diagCoeffTerm_le_of_le` and `splitCoeffReal_le_of_le` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_paperHugeWitness_of_eps1Slack_of_diagCoeff_of_bounds_of_gaps_of_le` in `repo/Twobites/IndependentSets.lean`, now let the remaining asymptotic work prove the huge-case bounds at any cleaner upper scale `B` once `(2 + ε₁) κ log log n ≤ B`.
- [x] Remove the remaining nat-level canonical-witness codegree hypothesis from the huge-case interface: `cast_choose_mul_le_paperK_of_real_bound` and `choose_mul_le_paperKNat_of_real_bound` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_paperHugeWitness_of_eps1Slack_of_le_of_codegCoeff` in `repo/Twobites/IndependentSets.lean`, now derive `paperHugeWitnessNat.choose 2 * codegreeBound ≤ k` from the chosen upper witness scale `B`, a real bound on the Section 3 codegree parameter, and one paper-scale coefficient inequality.
- [x] Fix the huge-case coefficient scale concretely at `3 * κ * log log n`: `paper_huge_deterministic_of_paperHugeWitness_of_eps1Slack_of_le_of_splitCoeff` in `repo/Twobites/IndependentSets.lean` now absorbs the canonical-witness choose/codegree term into the split coefficient plus final gap data, and `paper_huge_deterministic_of_paperHugeWitness_of_eps1Slack_of_three` specializes the whole huge-case wrapper to the explicit scale `3 * κ * log log n` under `ε₁ ≤ 1`.
- [x] Package the projected-codegree split error term at the `paperK` scale: `paperK_ratio_eq` and `cast_choose_mul_le_paperK_of_le_of_le` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_split_degree_le_paperPM_of_codegree_le_of_split_small` in `repo/Twobites/IndependentSets.lean`, now discharge the `choose(witnessSize, 2) * projCodegreeBound` huge-case hypothesis from a witness-cardinality bound and a direct projected-codegree estimate.
- [x] Package the projected-degree split error term at the paper scale `p m`: `paperP_mul_paperM_eq_paperK_div_paperS`, `mul_paperP_mul_paperM_eq_paperK_div_paperS`, and `cast_mul_le_paperK_of_le_of_le_paperP_mul_paperM` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_split_degree_le_paperPM_of_split_small` in `repo/Twobites/IndependentSets.lean`, now discharge the `witnessSize * degreeBound` huge-case hypothesis from a cardinality bound on the witness family and a projected-degree estimate of the form `degreeBound ≤ p m`.
- [x] Add a split-smallness bridge for the huge case in `repo/Twobites/IndependentSets.lean`: `cast_add_le_paperK_add_of_le_paperK` and `paper_huge_deterministic_of_split_error_le_paperK_of_split_small` now package the right-branch `3B` hypotheses from separate `paperK` bounds on `witnessSize * degreeBound` and `choose(witnessSize, 2) * projCodegreeBound`, so the remaining huge-case arithmetic is expressed entirely at the paper's `paperK` coefficient scale.
- [x] Add a `paperK`-to-`paperKNat` bridge for natural error terms and expose the huge-case theorem at the real-valued scale used in the paper: `nat_le_paperKNat_of_le_paperK` in `repo/Twobites/ParameterBounds.lean` and `paper_huge_deterministic_of_split_error_le_paperK` in `repo/Twobites/IndependentSets.lean` now let the remaining huge-case arithmetic be stated directly as `paperK` bounds on the degree and projected-codegree contributions.
- [x] Split the huge-case error term into separate `paperKNat` bounds for its degree and projected-codegree pieces: `add_le_paperKNat_of_le_paperKNat_of_le_paperKNat_of_one_le_gap_of_le` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_deterministic_of_split_error_le_paperKNat` in `repo/Twobites/IndependentSets.lean`, now package `B = witnessSize * degreeBound + choose(witnessSize, 2) * projCodegreeBound` via independent `paperKNat` bounds on the two summands before the final huge-case coefficient arithmetic.
- [x] Thread the new `+2` ceiling-gap absorption into the huge-case wrappers: `paperKNat_add_paperCapNat_add_paperKNat_le_paperKNat_of_two_le_gap_of_le` in `repo/Twobites/ParameterBounds.lean`, together with `paper_huge_blue_cross_deterministic_of_error_le_paperKNat_of_right_three_mul_error_le`, `paper_huge_red_cross_deterministic_of_error_le_paperKNat_of_right_three_mul_error_le`, and the assembled `paper_huge_deterministic_of_error_le_paperKNat` in `repo/Twobites/IndependentSets.lean`, now turn a `paperKNat` bound on the witness-error term into the additive huge-case deficit hypotheses automatically.
- [x] Add ceiling-sum and constant-gap absorption support for additive huge-case deficits in `repo/Twobites/ParameterBounds.lean`: `paperKNat_add_paperKNat_le_paperKNat_add_one`, `paperKNat_add_paperCapNat_add_paperKNat_le_paperKNat_add_two`, `paperKNat_add_two_le_paperKNat_of_two_le_gap`, and `paperKNat_add_paperCapNat_add_paperKNat_le_paperKNat_of_two_le_gap` now package the natural-number ceiling arithmetic needed to compare `paperKNat ρ + paperCapNat + B` against a single `paperKNat` target once a two-unit coefficient gap is available.
- [x] Reduce the huge-cross branchwise error hypotheses to explicit `3B`-smallness conditions in `repo/Twobites/IndependentSets.lean`: the generic algebra lemmas `cast_mul_add_choose_two_le_eps_mul_choose_two_of_le_of_three_mul_le` and `cast_mul_add_choose_two_le_eps_mul_choose_two_cap_add_choose_two_sub_of_le_of_three_mul_le`, together with the specialized blue/red left/right error lemmas and the wrappers `paper_huge_blue_cross_deterministic_of_card_le_of_one_le_gap_of_three_mul_error_le` / `paper_huge_red_cross_deterministic_of_card_le_of_one_le_gap_of_three_mul_error_le`, now package the huge cross terms once `B` is bounded below the available deficits and `3B` is small compared to the corresponding branch targets.
- [x] Push the huge-cross wrappers one step further toward pure parameter arithmetic in `repo/Twobites/IndependentSets.lean`: `three_mul_le_mul_sub_one_mono` and the wrappers `paper_huge_blue_cross_deterministic_of_card_le_of_paramDeficit_of_right_three_mul_error_le` / `paper_huge_red_cross_deterministic_of_card_le_of_paramDeficit_of_right_three_mul_error_le` now replace the actual `|\pi_R(I)|` / `|\pi_B(I)|` deficits by cleaner parameter deficits `paperKNat κ - paperKNat ρ - paperCapNat`, so the remaining huge-cross work is concentrated in explicit parameter inequalities.
- [x] Close the deterministic assembly of Paper Lemma `lem:huge` in `repo/Twobites/IndependentSets.lean`: `paper_huge_deterministic` now packages the diagonal huge red/blue bounds together with the new parameter-deficit huge blue/red cross-term wrappers, leaving only the remaining Section 3 parameter inequalities to instantiate its hypotheses.
- [x] Repackage the huge-cross parameter deficit in the additive form expected by later asymptotic coefficient comparisons: `paper_huge_blue_cross_deterministic_of_additiveParamDeficit_of_right_three_mul_error_le`, `paper_huge_red_cross_deterministic_of_additiveParamDeficit_of_right_three_mul_error_le`, and the assembled variant `paper_huge_deterministic_of_additiveParamDeficit` now consume `paperKNat ρ + paperCapNat + B ≤ paperKNat κ` directly.
- [x] Push the exact huge-cross cap-base absorption all the way to target-coefficient wrappers: `paperKNat_le_paperKNat_of_le` and `paperKNat_add_paperCapNat_le_paperKNat_of_one_le_gap_of_le` in `repo/Twobites/ParameterBounds.lean`, together with `redImage_card_add_paperCapNat_le_paperKNat_of_card_le_of_one_le_gap_of_le`, `blueImage_card_add_paperCapNat_le_paperKNat_of_card_le_of_one_le_gap_of_le`, and the corresponding huge-cross deterministic wrappers `paper_huge_blue_cross_deterministic_of_card_le_of_one_le_gap_of_witnessErrorBounds` / `paper_huge_red_cross_deterministic_of_card_le_of_one_le_gap_of_witnessErrorBounds` in `repo/Twobites/IndependentSets.lean`, now package the exact additive cap-base step under coefficient comparison hypotheses.
- [x] Add exact `+1`-absorption lemmas for the huge-cross cap-base step: `paperKNat_add_one_le_paperKNat_of_one_le_gap` and `paperKNat_add_paperCapNat_le_paperKNat_of_one_le_gap` in `repo/Twobites/ParameterBounds.lean`, together with the lifted image lemmas `redImage_card_add_paperCapNat_le_paperKNat_of_card_le_of_one_le_gap` and `blueImage_card_add_paperCapNat_le_paperKNat_of_card_le_of_one_le_gap` in `repo/Twobites/IndependentSets.lean`, now turn the earlier `+1` coefficient-summed image-plus-cap bounds into exact additive cap-base inequalities once a unit-sized gap on the remaining coefficient is supplied.
- [x] Add the first coefficient-summed cap-base arithmetic in `repo/Twobites/ParameterBounds.lean` and `repo/Twobites/IndependentSets.lean`: `natCeil_add_natCeil_le_natCeil_add_one`, `paperK_add`, and `paperKNat_add_paperCapNat_le_paperKNat_add_one` control the sum of a `paperKNat` image bound and `paperCapNat`, while `redImage_card_add_paperCapNat_le_paperKNat_add_one_of_card_le` and `blueImage_card_add_paperCapNat_le_paperKNat_add_one_of_card_le` lift that arithmetic to the actual `|\pi_R(I)| + cap` / `|\pi_B(I)| + cap` quantities.
- [x] Repackage the huge-cross cap-base hypothesis in `repo/Twobites/IndependentSets.lean` into the additive form that later asymptotic bounds naturally produce: `paper_huge_blue_cross_deterministic_of_paperCapNat_of_witnessErrorBounds_of_additiveCapBase` and `paper_huge_red_cross_deterministic_of_paperCapNat_of_witnessErrorBounds_of_additiveCapBase` now accept `|π_R(I)| + cap ≤ k` / `|π_B(I)| + cap ≤ k` instead of the subtraction-form cap-base inequality.
- [x] Extend `repo/Twobites/ParameterBounds.lean` with cap-to-`k` monotonicity lemmas: `paperK_le_paperK_of_le`, `paperCap_eq_paperK_scaled`, `paperCap_le_paperK_of_mul_le`, and `paperCapNat_le_paperKNat_of_mul_le` now compare the huge-case cap directly against the paper's `k` scale under coefficient inequalities.
- [x] Specialize the huge-cross deterministic wrappers in `repo/Twobites/IndependentSets.lean` to the paper cap `paperCapNat`: `paper_huge_blue_cross_deterministic_of_paperCapNat_of_witnessErrorBounds` and `paper_huge_red_cross_deterministic_of_paperCapNat_of_witnessErrorBounds` now replace the raw `paperHuge*CrossErrorNat` inequalities by witness-size error bounds plus the remaining cap-base and branchwise witness-error hypotheses.
- [x] Relate the huge-case cap to the paper's `k` scale in `repo/Twobites/ParameterBounds.lean`: `paperP_mul_n_eq_paperK`, `paperCap_eq_mul_paperK`, and `paperCapNat_le_mul_paperK_add_one` put `paperCap` / `paperCapNat` on the same asymptotic footing as `paperK`.
- [x] Reduce the huge cross-term error sizes from exact `H_I` filters to witness-size bounds in `repo/Twobites/IndependentSets.lean`: `paperHugeBlueCrossErrorNat_le_of_goodEventD_of_paperWitness` and `paperHugeRedCrossErrorNat_le_of_goodEventD_of_paperWitness`, together with the monotonicity lemmas `cast_choose_two_le_of_le` and `cast_mul_add_choose_two_le_of_le`, isolate the remaining huge-case work to pure parameter arithmetic.
- [x] Add a project-level interface for the paper's huge-case cap in `repo/Twobites/ParameterBounds.lean`: `paperCap`, `paperCapNat`, and their basic nonnegativity / ceiling comparison lemmas package the expression `(1+\varepsilon_2)pn` for later use in Lemma `lem:huge`.
- [x] Prove exact `choose 2` error-expansion lemmas and use them to compare the huge cross concrete bounds against the paper targets branchwise in `repo/Twobites/IndependentSets.lean`: `cast_min_choose_two_add_le_one_add_mul_min`, `paperHugeBlueCrossConcreteBoundNat_le_target_of_error_bounds`, `paperHugeRedCrossConcreteBoundNat_le_target_of_error_bounds`, and the deterministic wrappers `paper_huge_blue_cross_deterministic_of_error_bounds` / `paper_huge_red_cross_deterministic_of_error_bounds` now reduce the remaining huge-case work to two explicit small-error inequalities per color.
- [x] Add ceiling upper-bound helpers in `repo/Twobites/ParameterBounds.lean`: `paperKNat_le_paperK_add_one`, `ceil_paperT1_le_paperT1_add_one`, `ceil_paperT2_le_paperT2_add_one`, and `ceil_paperT3_le_paperT3_add_one` package the ubiquitous `\lceil x \rceil \le x + 1` step with the needed nonnegativity hypotheses.
- [x] Add final-comparison wrappers for the huge cross terms in `repo/Twobites/IndependentSets.lean`: `paper_huge_blue_cross_deterministic` and `paper_huge_red_cross_deterministic` now reduce Lemma `lem:huge`'s cross-projection estimates to explicit comparisons between the current concrete `paperKNat`-level bounds and the paper's target min-expressions.
- [x] Isolate the remaining sign/additive witness bookkeeping in `repo/Twobites/ParameterBounds.lean`: `paperT1_nonneg_of_loglog_nonneg` discharges the large-part nonnegativity hypothesis under the paper's log-log assumption, and the `paperKNat_lt_mul_ceil_paperT{i}_sub_choose_mul_iff` lemmas put the witness inequalities into the additive form expected by the later asymptotic estimates.
- [x] Lift the huge-case cross-projection bounds in `repo/Twobites/IndependentSets.lean` to the same paper-witness level as the diagonal huge lemmas: `paper_huge_blue_cross_concrete_of_paperWitness` and `paper_huge_red_cross_concrete_of_paperWitness` now bound the `H_I \cap V_R` and `H_I \cap V_B` cross terms by concrete `paperKNat - |\pi_R(I)|` / `paperKNat - |\pi_B(I)|` min-expressions with explicit degree/codegree error terms.
- [x] Add additive-form witness arithmetic helpers in `repo/Twobites/ParameterBounds.lean`: the hypotheses `paperKNat < witnessSize * \lceil t_i \rceil - \binom{witnessSize}{2}D` are now interchangeable with the more natural `paperKNat + \binom{witnessSize}{2}D < witnessSize * \lceil t_i \rceil` form for `t_1`, `t_2`, and `t_3`.
- [x] Replace the huge-case cross-projection union-error interface in `repo/Twobites/IndependentSets.lean` by concrete `|I|-|\pi_R(I)|` / `|I|-|\pi_B(I)|` bridge lemmas: projection unions are now identified with projected images of `\bigcup X_v(I)`, bounded by complement-cardinality identities, and propagated under `GoodEventD` to new concrete weight and `min`-pair-count bounds with explicit deterministic error terms.
- [x] Add the abstract huge-case `min`-expression bounds in `repo/Twobites/IndependentSets.lean`: the red/blue cross-projection pair counts now satisfy `min { choose(total), choose(cap) + choose(total - cap) }` bounds once supplied with a per-vertex cap, a lower bound placing the cap below the total weight, and the existing projection-union-plus-intersection-error estimate.
- [x] Add the capped-sum convexity theorem in `repo/Twobites/IndependentSets.lean`: if each term is at most a cap and the total exceeds that cap, then `sum choose 2` is bounded by `choose cap + choose (total - cap)`, with projection-specialized corollaries for the red/blue huge cross-projection sums.
- [x] Add the convexity/choose-of-total-weight step for the huge-case projection sums in `repo/Twobites/IndependentSets.lean`: `partPairCount`, `redProjectionPairCount`, and `blueProjectionPairCount` are now bounded by `choose` of their total weights, and the red/blue huge cross-projection sides already reduce under `GoodEventD` to `choose` of the new projection-union-plus-intersection-error quantities.
- [x] Add paper-facing deterministic aliases in `repo/Twobites/IndependentSets.lean` for the current Section 3 support results: `paper_large_deterministic`, `paper_medium_deterministic`, `paper_huge_red_diagonal_deterministic`, and `paper_huge_blue_diagonal_deterministic` now package the `GoodEventD` bounds with the new `|I| \le \lceil k \rceil` witness arithmetic interface.
- [x] Extend the huge-case projection bookkeeping in `repo/Twobites/IndependentSets.lean` with `redProjectionUnion` / `blueProjectionUnion` and the corresponding union-plus-pairwise-intersection bounds, including the `GoodEventD` specializations for `H_I \cap V_R` and `H_I \cap V_B` that start the cross-projection argument behind Lemma `lem:huge`.
- [x] Add `|I| \le \lceil k \rceil` witness wrappers in `repo/Twobites/IndependentSets.lean`: the large, medium, and diagonal huge `GoodEventD` lemmas now accept a parameter-only witness inequality on `\lceil k \rceil` and package direct `\varepsilon_1 k^2` conclusions once the remaining Section 3 arithmetic bounds are supplied.
- [x] Add the first witness-inequality-to-contribution lemmas in `repo/Twobites/IndependentSets.lean`: `GoodEventD` now gives large/medium and diagonal-huge bounds once a Section 3 witness-size inequality is supplied, plus the filter-cardinality helper lemmas needed for `H_I ∩ V_R` and `H_I ∩ V_B`.
- [x] Extend `repo/Twobites/ParameterBounds.lean` with ceiling-level threshold arithmetic (`ceil_paperT1_le_paperKNat`, `ceil_paperT2_le_paperKNat`, `ceil_paperT3_le_ceil_paperT2`, `ceil_paperT2_le_ceil_paperT1_of_loglog_le`) so the new witness/cardinality lemmas have the right natural-number interfaces.
- [x] Add the witness-subset/cardinality infrastructure in `repo/Twobites/IndependentSets.lean`: lower-bound the `partWeight` of a family from a natural lower bound on every `|X_v(I)|`, extract fixed-size witness subsets with `Finset.exists_subset_card_eq`, and deduce `L_I`, `M_I`, and `H_I` cardinality bounds from `GoodEventD` once the corresponding threshold inequalities are supplied.
- [x] Clean the current `repo/Twobites/IndependentSets.lean` linter warnings while extending the `GoodEventD` machinery.
- [x] Encode the deterministic good event `𝒟` in `repo/Twobites/IndependentSets.lean` as `GoodEventD`, and derive from it the inherited `X_v(I)` codegree bounds, projected codegree bounds, and `partWeight` / projection-weight upper bounds needed by the counting lemmas.
- [x] Turn the earlier explicit-hypothesis wrappers into actual deterministic support lemmas in `repo/Twobites/IndependentSets.lean`: `GoodEventD` now yields raw large/medium/small contribution bounds and the diagonal huge red/blue contribution bounds on `H_I ∩ V_R` and `H_I ∩ V_B`.
- [x] Add the first paper-style contribution wrappers under explicit weight hypotheses: large/medium/small `partPairCount` bounds and huge red/blue projection-pair bounds in `repo/Twobites/IndependentSets.lean`.
- [x] Isolate the remaining analytic comparison for `t_2 ≤ t_1` in `repo/Twobites/ParameterBounds.lean` as a reusable reduction lemma from a log-log growth hypothesis.
- [x] Add the partition-usage lemmas in `repo/Twobites/IndependentSets.lean`: every base vertex falls into one of `H_I/L_I/M_I/S_I`, the adjacent regimes are disjoint unconditionally, and the remaining disjointness statements are isolated behind the threshold-order hypotheses they need.
- [x] Add the first huge-regime projection contribution bounds in `repo/Twobites/IndependentSets.lean` via `redProjectionPairCount`, `blueProjectionPairCount`, and their `|I|/2` upper bounds in terms of projection weights.
- [x] Add the first actual deterministic counting lemmas in `repo/Twobites/IndependentSets.lean`: `partWeight`, `partPairCount`, projection-weight sums, and `choose 2` upper bounds specialized to `H_I`, `L_I`, `M_I`, and `S_I`.
- [x] Strengthen `repo/Twobites/ParameterBounds.lean` with the first concrete threshold-order inequalities, including `t_3 ≤ t_2` and explicit bounds placing `t_1` and `t_2` below `k` under large-`n` / small-`ε` hypotheses.
- [x] Upgrade the local deletion lemmas in `repo/Twobites/Construction.lean` into triangle-free / `CliqueFree 3` theorems for `retainedRed`, `retainedBlue`, and `finalGraph`.
- [x] Extend `repo/Twobites/IndependentSets.lean` to the paper's `H_I`, `L_I`, `M_I`, `S_I` partition, projected-image bookkeeping, and restricted `ClosedPairOn` / `ClosedPairPlusOn` predicates.
- [x] Extend `repo/Twobites/ParameterBounds.lean` with the paper's Section 3 thresholds `t_1`, `t_2`, `t_3` and their basic positivity lemmas.
- [x] Prove the first deterministic deletion lemmas in `repo/Twobites/Construction.lean`: deleted edges cannot survive retained color layers, and the later edge of a monochromatic triangle cannot survive in `finalGraph`.
- [x] Start `repo/Twobites/IndependentSets.lean` with the paper's `X_v(I)`, `X_v^+(I)`, closed/open pair predicates, and witness-to-closed-pair lemmas.
- [x] Extend `repo/Twobites/Construction.lean` through the paper's triangle-deletion bookkeeping: ordered coordinate pairs, monochromatic and mixed deletion witnesses, retained color layers, and the final simple graph.
- [x] Make the bridge between natural graph parameters and real asymptotic abbreviations explicit in `repo/Twobites/ParameterBounds.lean` via `paperSNat`, `paperMNat`, `paperKNat`, and comparison lemmas.
- [x] Add the first deterministic support layer in `repo/Twobites/Construction.lean`: construction data, projections, finite-set images, fibers, and the raw red/blue/simple lifted graphs.
- [x] Add the first parameter support layer in `repo/Twobites/ParameterBounds.lean`: paper-style definitions of `s`, `m`, `p`, `k` together with basic positivity and squaring lemmas.
- [x] Add the generic total-budget RI wrappers in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_totalBudget_of_le`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_totalBudget_of_le`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_totalBudget_of_le` now collapse the current `(remaining, u_R, u_B)` shell to exponentials once a single natural-number budget inequality is supplied for each good graph pair.
- [x] Add the direct `section4F` total-error RI wrappers in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_section4FTotalError`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_section4FTotalError`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_section4FTotalError` now repackage the filtered RI shell directly in the natural Section 4 loss/count coordinates `reveal + 3 * LMS + huge`, so the remaining blocker is to prove that concrete total-error inequality for each good graph pair from the checked three-pool witness decomposition.
- [x] Add the direct manuscript-loss-gap pointwise `section4F` RI endpoint in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_section4FTotalError_of_paperRISILossGap_of_paperSection4Bound` now turns one good graph pair's literal Section 4 total-error inequality together with the manuscript `paperRISILossGap` bound into the exponential bad-event estimate without reopening the intermediate target-gap cast arithmetic, so the remaining combinatorial work can target exactly those two graph-dependent hypotheses.
- [x] Lift the direct manuscript-loss-gap `section4F` endpoint through the filtered embedding and fixed-set sums in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_section4FTotalError_of_paperRISILossGap_of_paperSection4Bound` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_section4FTotalError_of_paperRISILossGap_of_paperSection4Bound` now propagate the same literal `hTotal` + `paperRISILossGap` graph-pair interface through the current RI mass shells, so later combinatorial lemmas can collapse the fixed-embedding/fixed-set sums without routing back through the intermediate target-gap bridge.
- [x] Factor the embedding-only image-cardinality hypotheses out of the direct `section4F` + manuscript-loss-gap shell in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_section4FTotalError_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_section4FTotalError_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds` now let the remaining combinatorial collapse target only the genuinely graph-dependent `hTotal`, `paperRISILossGap`, `SPart`, and opposite-color projection-cap hypotheses for one good graph pair.
- [x] Carry the direct `section4F` + manuscript-loss-gap route through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_section4FTotalError_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_lt_explicitGoodMass` now packages that manuscript-native route from graph-pair exponent bounds all the way to the witness-existence theorem, leaving only the genuinely graph-dependent fixed-embedding collapse to supply the remaining hypotheses uniformly.
- [x] Add the natural-number pointwise direct `section4F` shell in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound` now accepts the key Section 4 total-error hypothesis as a literal natural-number inequality before the final cast to `ℝ`, so the remaining three-pool/cardinality proof can stay in naturals at the graph-pair level.
- [x] Lift the natural-number direct `section4F` shell through the filtered embedding and fixed-set sums in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound`, `..._of_embeddingImageBounds`, `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound`, and `..._of_embeddingImageBounds` now keep the direct manuscript-native Section 4 total-error hypothesis in naturals all the way through the current fixed-embedding and fixed-set bad-event mass shells, so the remaining three-pool/cardinality collapse can stay in nat-valued counting coordinates up to the choose-multiplied mass estimate.
- [x] Add the deterministic-bounds nat-valued direct `section4F` wrappers in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound_of_paperDeterministicBounds`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` now specialize the nat-valued total error to the literal `reveal + 3 * LMS + huge` count and discharge the routine `12 * ε1 * k^2` bound internally from the existing deterministic large/medium/small/huge `GoodEventD` estimates, so the nat manuscript-native route now exposes only the genuinely graph-dependent loss-gap, `SPart`, and opposite-color cap hypotheses above the graph-pair level.
- [x] Extract the deterministic `12 * ε1 * k^2` total-error package for the nat manuscript-native route in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_section4FTotalErrorNat_cast_le_twelve_mul_eps_mul_paperKSq_of_paperDeterministicBounds` now isolates the routine large/medium/small/huge `GoodEventD` contribution bounds for the literal nat-valued `reveal + 3 * LMS + huge` expression, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound_of_paperDeterministicBounds` has been refactored to consume this helper instead of rebuilding the same deterministic arithmetic inline.
- [x] Extract the casted actual-loss-plus-witness-caps inequality from the literal LMS total-error shell in `repo/Twobites/IndependentSets.lean`: `section4SecondStageLossNat_add_witnessCaps_cast_le_totalError_of_LMS_totalError` now packages the `exact_mod_cast` step from `section4SecondStageLossNat_add_witnessCaps_le_revealBudget_add_three_mul_partPairCount_LMS_add_huge` into a reusable theorem, and `section4ProjectionChoiceMassSum_section4F_le_exp_of_LMS_totalError` has been refactored to consume that helper instead of rebuilding the same coercion arithmetic inline.
- [x] Discharge the residual `HPart ⊆ F₂` side condition directly at the good-graph-pair Section 4 loss layer in `repo/Twobites/IndependentSets.lean`: the new helpers `goodSurvivingGraphPair_section4SecondStageLossNat_le_revealBudget_add_two_mul_partPairCount_LMS_add_huge_of_paperSection4Bound` and `goodSurvivingGraphPair_section4SecondStageLossNat_add_witnessCaps_cast_le_totalError_of_LMS_totalError_of_paperSection4Bound` now package the literal Section 4 loss and loss-plus-opposite-witness-cap inequalities for one good graph pair with the `HPart ⊆ F₂` obligation discharged automatically from `GoodEventD`, so the remaining three-pool combinatorics can target those counting statements directly.
- [x] Add the final extraction wrapper for the deterministic-bounds nat-valued direct `section4F` route in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_section4FTotalErrorNat_of_paperRISILossGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now carries that nat manuscript-native shell all the way from the filtered bad-event mass estimate to the explicit-good-mass existence theorem, so the remaining obstruction is purely the genuinely graph-dependent fixed-embedding collapse supplying the loss-gap, `SPart`, and opposite-color cap hypotheses.
- [x] Add the direct deterministic-bounds near-one wrappers in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, and `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now push the near-one branch through the stronger direct deterministic-bounds manuscript `paperRISILossGap` shell all the way to the explicit-good-mass witness theorem, so the remaining near-one obstruction is again only the genuinely graph-dependent fixed-embedding collapse.
- [x] Factor the repeated deterministic good-graph-pair reveal/L/M/S/huge-diagonal package into the reusable helper `section4UniformContributionBounds_of_paperDeterministic` in `repo/Twobites/IndependentSets.lean`, and refactor `section4LossTargetNat_le_ceil_nine_mul_eps_mul_paperKSq_of_paperDeterministic`, `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperDeterministicTargetGap_of_paperSection4Bound`, `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap`, and `goodSurvivingGraphPair_section4FTotalErrorNat_cast_le_twelve_mul_eps_mul_paperKSq_of_paperDeterministicBounds` to consume it. This centralizes the current huge-case wrapper dependency and leaves the remaining RI obstruction purely in the genuinely graph-dependent three-pool collapse.
- [x] Add the reduced uniform-paper-error manuscript-loss-gap bridge at graph-pair level in `repo/Twobites/IndependentSets.lean`: `section4LossTargetNat_le_ceil_nine_mul_eps_mul_paperKSq_of_uniformBounds` now exposes the nat-valued `9 * ε1 * k^2` loss estimate directly from the reduced uniform Section 4 error hypotheses, `section4LossLeTarget_of_paperRISILossGap_of_uniformPaperErrorBounds` packages the corresponding arithmetic step from `paperRISILossNat` to the literal `hLossLeTarget` inequality on one construction, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound` feeds that lighter graph-pair bridge into the existing reduced deterministic-target-gap shell. The remaining collapse can therefore target either the direct manuscript-loss-gap route or the reduced uniform-paper-error target-gap route without reopening the heavier cap-weight deterministic interface.
- [x] Extract the standalone good-graph-pair `hLossLeTarget` endpoint for the reduced uniform-paper-error route in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_section4LossLeTarget_of_paperRISILossGap_of_uniformPaperErrorBounds` now isolates the literal natural target-gap inequality on one good graph pair from `paperRISILossNat`, the reveal-budget witness bound, and the uniform large/medium/small/huge pair-count hypotheses, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound` has been refactored to consume that helper instead of rebuilding the same loss-gap-to-target arithmetic locally.
- [x] Lift the reduced uniform-paper-error manuscript-loss-gap route through the fixed-embedding and fixed-set RI mass shells in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound` now let later combinatorial lemmas stay in manuscript `paperRISILossGap` coordinates, while reusing the lighter reduced uniform-paper-error target-gap shell internally instead of routing back through the heavier cap-weight deterministic interface.
- [x] Carry the reduced uniform-paper-error manuscript-loss-gap route through the deterministic-bounds and explicit-good-mass layers in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, and `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now preserve that reduced manuscript interface all the way to the final witness-existence theorem while discharging the routine deterministic Section 4 contribution bounds internally.
- [x] Add the reusable good-graph-pair deterministic Section 4 contribution package in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_section4UniformContributionBounds_of_paperDeterministicBounds` now exposes the reveal/L/M/S/huge-diagonal bounds directly from `goodSurvivingGraphPairPred`, and the pointwise deterministic target-gap shell, the direct manuscript-loss-gap shell, and the nat-valued `section4F` total-error shell have been refactored to consume it instead of rebuilding the same `GoodEventD` unpacking and repeated `ConstructionData` substitutions locally. This leaves the remaining RI obstruction more cleanly focused on the genuinely graph-dependent manuscript loss-gap / opposite-color cap / huge-cross-count / nat total-error hypotheses coming from the checked three-pool witness decomposition.
- [x] Add the raw nat-valued good-graph-pair witness-cap inequality in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_section4SecondStageLossNat_add_witnessCaps_le_revealBudget_add_three_mul_partPairCount_LMS_add_huge_of_paperSection4Bound` now exposes the literal `section4SecondStageLossNat + u_R + u_B` bound directly from `goodSurvivingGraphPairPred`, and the existing casted helper `goodSurvivingGraphPair_section4SecondStageLossNat_add_witnessCaps_cast_le_totalError_of_LMS_totalError_of_paperSection4Bound` has been refactored to flow through that nat theorem instead of re-invoking the generic `ConstructionData` shell. This gives the remaining three-pool collapse a graph-pair-native nat inequality to target before the final cast to `ℝ`.
- [x] Add the full huge diagonal/cross graph-pair deterministic bridge in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_hugeContributionBounds_of_paperDeterministicBounds` now exposes the four huge-regime bounds directly from `goodSurvivingGraphPairPred`, and the new pointwise theorem `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_paperDeterministicHugeBounds` uses that bridge to feed the reduced manuscript-loss-gap route without asking callers for the huge diagonal/cross bounds separately. This narrows the remaining graph-pair frontier to the literal manuscript loss-gap, opposite-color cap, huge-cross-count, and nat total-error hypotheses coming from the checked three-pool decomposition.
- [x] Bundle the full graph-pair deterministic paper-error package in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_uniformPaperErrorBounds_of_paperDeterministicBounds` now combines the reveal/L/M/S/huge-diagonal bounds from `goodSurvivingGraphPair_section4UniformContributionBounds_of_paperDeterministicBounds` with the huge-cross bounds from `goodSurvivingGraphPair_hugeContributionBounds_of_paperDeterministicBounds` in the exact eight-field shape used by the reduced manuscript/target-gap shells, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperDeterministicTargetGap_of_paperSection4Bound` has been refactored to consume that bundled helper instead of rebuilding the same deterministic package locally.
- [x] Add graph-pair wrappers for the uniform-paper-error open-pair target-gap bounds in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_paperSection4OpenPairTargetNat_sub_openError_le_baseOpenPairSet_card_of_uniformPaperErrorBounds` and `goodSurvivingGraphPair_paperSection4OpenPairTargetNat_sub_ceil_ten_mul_eps_mul_paperKSq_le_baseOpenPairSet_card_of_uniformPaperErrorBounds` now expose the literal natural-number Section 4 open-pair target-gap inequalities directly from `goodSurvivingGraphPairPred` together with the bundled large/medium/small/huge paper-error hypotheses, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperDeterministicTargetGap_of_uniformPaperErrorBounds_of_paperSection4Bound` has been refactored to consume that graph-pair wrapper instead of reopening the underlying `ConstructionData` package locally.
- [x] Add the direct deterministic-bounds graph-pair target-gap bridge for the reduced manuscript route in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_section4LossLeTarget_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperDeterministicBounds` now packages the literal nat-valued `hLossLeTarget` inequality directly from `goodSurvivingGraphPairPred`, manuscript `paperRISILossGap`, and the deterministic Section 4 witness hypotheses, and `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap_of_uniformPaperErrorBounds_of_paperSection4Bound_of_paperDeterministicBounds` has been refactored to use that bridge before feeding the reduced deterministic-target-gap endpoint. This makes the literal graph-pair target-gap inequality itself available as a reusable deterministic-bounds theorem for the remaining fixed-embedding collapse.
- [x] Add the generic deterministic-bounds graph-pair open-error target-gap wrapper in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_paperSection4OpenPairTargetNat_sub_openError_le_baseOpenPairSet_card_of_paperDeterministicBounds` now exposes the literal natural-number `paperSection4OpenPairTargetNat - openError ≤ |O(I)|` inequality directly from `goodSurvivingGraphPairPred`, with caller-supplied red/blue closed-pair error budgets. This gives the remaining three-pool collapse a graph-pair-native deterministic bridge at the exact open-pair target-gap level before specializing to the manuscript `⌈10 ε₁ k²⌉` error shell.
- [x] Add the generic deterministic-bounds target-gap wrapper family in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_targetGap_of_paperSection4Bound_of_paperDeterministicBounds`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_targetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_targetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` now internalize the routine deterministic reveal/L/M/S/huge-diagonal contribution package while leaving only the graph-dependent `hLossLeTarget` and open-pair target-gap hypotheses quantified above graph-pair level. This gives the pending three-pool collapse a lighter reduced interface that can target arbitrary natural-number `openError` budgets without reopening the deterministic contribution proofs.
- [x] Carry the generic deterministic-bounds arbitrary-`openError` target-gap route through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_targetGap_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now packages that lighter target-gap interface all the way to the witness-existence theorem, so the remaining three-pool collapse can aim directly at arbitrary natural-number `openError` target-gap budgets without any wrapper gap above the fixed-set layer.
- [x] Add the reduced manuscript-loss-gap wrappers with split closed-pair error budgets in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_paperRISILossGap_of_splitClosedErrorBounds_of_paperSection4Bound_of_paperDeterministicBounds`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_of_splitClosedErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_of_splitClosedErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` now combine the reduced manuscript `paperRISILossGap` route with caller-supplied red/blue/open closed-pair error budgets, provided `openError ≤ ⌈10 ε₁ k²⌉`. This lets the remaining three-pool collapse feed the reduced manuscript shells directly with split red/blue closed-error estimates instead of rebuilding an intermediate target-gap theorem by hand.
- [x] Carry the reduced manuscript-loss-gap route with split closed-pair error budgets through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperRISILossGap_of_splitClosedErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now packages that split-error interface all the way to the witness-existence theorem, so the remaining three-pool collapse can target split red/blue/open closed-error budgets directly without any wrapper gap above the fixed-set layer.
- [x] Add the lighter non-deterministic split-closed-error wrapper family in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperRISILossGap_of_splitClosedErrorBounds_of_paperSection4Bound`, `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperRISILossGap_of_splitClosedErrorBounds_of_paperSection4Bound`, and `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperRISILossGap_of_splitClosedErrorBounds_of_paperSection4Bound_lt_explicitGoodMass` now keep the reduced manuscript `paperRISILossGap` route above graph-pair level while accepting literal split red/blue/open closed-pair error budgets directly, without reintroducing the stronger deterministic-bounds packaging outside the graph-pair shell.
- [x] Add reusable good-graph-pair deterministic target-gap wrappers in `repo/Twobites/IndependentSets.lean`: `goodSurvivingGraphPair_paperSection4OpenPairTargetNat_sub_ceil_ten_mul_eps_mul_paperKSq_le_baseOpenPairSet_card_of_paperDeterministicBounds` and `goodSurvivingGraphPair_paperSection4OpenPairTarget_sub_ceil_ten_mul_eps_mul_paperKSq_le_baseOpenPairSet_card_of_paperDeterministicBounds` now expose the rounded Section 4 target-gap bounds directly from `goodSurvivingGraphPairPred`, and the pointwise deterministic target-gap / manuscript-loss-gap shells have been refactored to consume them instead of rebuilding the same `GoodEventD` and independence unpacking locally. This trims another repeated proof block from the graph-pair frontier and leaves the remaining work concentrated on the genuinely graph-dependent three-pool hypotheses.
- [x] Add the reduced uniform-paper-error near-one wrappers in `repo/Twobites/IndependentSets.lean`: `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds` now feed the near-one graph-pair inequality through the lighter reduced manuscript `paperRISILossGap` shell rather than the heavier direct deterministic manuscript route. This means the near-one branch now reaches fixed-embedding and fixed-set level in the reduced uniform-paper-error coordinates as well.
- [x] Carry the reduced uniform-paper-error near-one route through the final explicit-good-mass extraction step in `repo/Twobites/IndependentSets.lean`: `exists_triangleFreeWithSmallIndepNum_of_paperChooseMulLe_sum_if_good_sectionExp_of_paperNearOneReal_of_two_div_le_loglog_of_uniformPaperErrorBounds_of_paperSection4Bound_of_embeddingImageBounds_of_paperDeterministicBounds_lt_explicitGoodMass` now packages that reduced near-one branch all the way to the witness-existence theorem, so the remaining work can stay focused on the actual graph-dependent three-pool collapse rather than any missing wrapper surface.
- [x] Remove the residual `H_I ⊆ F_2` plumbing from the direct `section4F` total-error shell: `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_section4FTotalError_of_paperSection4Bound`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_section4FTotalError_of_paperSection4Bound`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_section4FTotalError_of_paperSection4Bound` now discharge that side condition automatically from `GoodEventD` using the existing paper Section 4 bound, so the remaining RI work is only the literal Section 4 loss/count inequality itself.
- [x] Add the target-gap wrappers on top of the new `section4F` total-error shell: the pointwise theorems `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_uniformError_of_revealWitnessBounds_of_targetGap`, `..._of_paperLargeWitness_of_paperHugeWitness_of_targetGap`, and `..._of_targetGap_of_paperSection4Bound` now turn the literal Section 4 loss/count inequality plus a paper target-gap inequality into the manuscript-scale exponential bound for one good graph pair, the lifted shells `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_targetGap_of_paperSection4Bound` / `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_targetGap_of_paperSection4Bound` propagate that same target-gap interface through the filtered fixed-embedding and fixed-set mass reductions, and the new composed wrappers `goodSurvivingGraphPairProjectionChoiceMassBound_le_exp_of_section4FTotalError_of_targetGap_of_paperSection4Bound`, `constructionEmbeddingUniformWeight_mul_paperGoodSurvivingGraphPairMass_le_outerMass_mul_sum_if_good_sectionExp_of_section4FTotalError_of_targetGap_of_paperSection4Bound`, and `paperConstructionMass_goodSurvivingIndepSetEventSet_le_sum_if_good_sectionExp_of_section4FTotalError_of_targetGap_of_paperSection4Bound` now let later combinatorial lemmas feed that shell with a direct Section 4 total-error bound together with a paper target-gap bound, without separately rebuilding the intermediate `hLossLeTarget` inequality.
- [x] Put the substantive paper-facing definitions and theorem statements directly in `repo/PaperDefinitions.lean` and `repo/PaperTheorems.lean`, while keeping the `repo/Twobites/` files buildable.
- [x] State the paper-facing main theorem, Ramsey lower-bound theorem, and witness-form Ramsey corollary as Lean propositions with no axioms or `sorry`s.
- [x] Write `repo/PLAN.md` for the main-results formalization, including statement choices, imports, reusable definitions, and dependency order.
- [x] Read `repo/paper/reference.tex` end to end, with the paper-check focused on the main-result sections.
- [x] Check the proof chain for Theorem `main`, including Lemmas `lem:fiber_and_degree`, `lem:large`, `lem:med`, `lem:small`, `lem:huge`, `lem:RISI`, and `lem:RI`.
- [x] Record corrections, hidden assumptions, and proof clarifications in `repo/PAPERNOTES.md`.
- [x] Confirm that no genuine gap was found in the main-result argument; the hypergraph section is only a proof sketch and remains out of scope for this formalization project.
