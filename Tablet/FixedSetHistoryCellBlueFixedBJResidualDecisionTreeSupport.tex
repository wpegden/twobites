\begin{helper}
For fixed support labels \(B,J\) in the blue-branch setup, suppose the
released residual partition data and cylinder packing hypotheses are
available.  Then the realized branch/transcript leaves form a finite
decision-tree support: every realized leaf has an associated released block,
distinct realized leaves have disjoint blocks, and the cylinder mass assigned
to a leaf is bounded by the mass of its released block.
\end{helper}
\begin{proof}
SKETCH:
Apply the released-block partition data from
\(\noderef{FixedSetHistoryCellBlueFixedBJResidualPartitionData}\) and the
subprobability packing estimate
\(\noderef{FixedSetHistoryCellBlueReleasedCylinderPacking}\).  The former
identifies the finite realized leaves and their released residual blocks; the
latter converts pairwise disjointness and nonnegative assignment mass into
the required decision-tree support bound.
\end{proof}
