\begin{lemma}
For any target set size $S$ and required choices $h$, if $S < h$ then the favorable row count is zero.
\end{lemma}
\begin{proof}
By properties of the falling factorial $(S)_h$, we have $(S)_h = 0$ when $h > S$.
\end{proof}
