\begin{definition}
For a graph \(G\) on \(m\) labeled vertices, \(\mathrm{GnpGraphWeight}(p,G)\)
is the \(G(m,p)\) product weight: every unordered pair contributes \(p\) if it
is an edge of \(G\) and \(1-p\) otherwise.
\end{definition}
