\begin{helper}
For fixed support labels \(B,J\) in the blue-branch residual setup, the
partition data, released-block data, and decision-tree support combine into a
single owner package.  The package consists of finite realized leaves,
released residual blocks, released block masses, pairwise disjointness of the
blocks, a subprobability bound for their total mass, and the per-leaf lower
bound coming from the released cylinder.
\end{helper}
\begin{proof}
SKETCH:
Combine
\(\noderef{FixedSetHistoryCellBlueFixedBJResidualReleasedBlockData}\),
\(\noderef{FixedSetHistoryCellBlueFixedBJResidualPartitionData}\), and
\(\noderef{FixedSetHistoryCellBlueFixedBJResidualDecisionTreeSupport}\).
The released-block data supplies the blocks and compatibility equivalence,
the partition node supplies the mass definitions and lower bounds, and the
decision-tree support node packages the finite disjoint support and total
mass estimate.
\end{proof}
