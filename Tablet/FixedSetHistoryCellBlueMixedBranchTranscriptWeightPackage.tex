\begin{helper}
In the fixed history cell, fix a red support label \(B\) and a blue support
label \(J\).  The blue-branch transcript construction, the fixed-\((B,J)\)
residual owner package, the released-cylinder packing estimate, and the
finite support-mass bounds imply the mixed branch/transcript weight package:
the total contribution of all realized branch/transcript cells for this
fixed pair is bounded by the product of the prescribed red and blue support
weights, with the selected-absence factors accounted for by the released
residual cylinders.
\end{helper}
\begin{proof}
SKETCH:
The cells and their geometry come from
\(\noderef{FixedSetHistoryCellBlueBranchTranscriptCells}\).  The residual
owner package supplies disjoint released blocks and lower bounds for the
released cylinders.  The support-determined-by-trace lemma
\(\noderef{FixedSetHistoryCellRedSupportDeterminedByBlueTrace}\) identifies
duplicate red-support decodings along equal blue traces.  The finite
summation lemmas \(\noderef{FinsetBlueFiberRedSupportMass}\) and
\(\noderef{FinsetBlueSupportMassBound}\) sum the remaining blue-trace
choices.
Combining these estimates with the fixed-cylinder charge gives the displayed
mixed weight bound.
\end{proof}
