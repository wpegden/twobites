\begin{definition}
For a finite simple graph $G$ and a real number $x$, the predicate
\(\mathrm{IndependenceNumberLess}(G,x)\) means that every independent vertex set
of \(G\) has cardinality strictly smaller than \(x\).
\end{definition}
